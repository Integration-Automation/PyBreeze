# 2026-09 update log, continued a third time

Continues [2026-09-c.md](2026-09-c.md), which passed the batch size limit. Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260925-116 · 2026-09-25 · Start the 2026-09-d batch · #docs

- **What**: `2026-09-c.md` had reached 970 lines, past the batch size limit of about 800 (Batch rules, item 2). Its 115 entries, the batch table's count and the index rows agree, as they do for `2026-09.md` (218) and `2026-09-b.md` (120).
- **Change**: new entries go into `2026-09-d.md`, listed in the batch table.
- **Files**: `docs/updates/2026-09-d.md`, `docs/updates/README.md`
- **Open items**: none.

## U-20260925-117 · 2026-09-25 · busy_cursor moves out of the menus into pybreeze_ui · #refactor

- **What**: `busy_cursor()` was written into `menu/menu_utils.py` for the menu entries (U-20260925-114), and the tool tabs are to use it next for what they do with a large input; a tool tab importing from the menus' helpers would run against how the layers depend on each other.
- **Change** (refactor, no behaviour change): it lives in `pybreeze/pybreeze_ui/busy_cursor.py`, beside the other UI helpers (`plain_text.py`, `closing.py`); `menu_utils.py`, the Tools menu, the automation menu factory, the JupyterLab tab entry and `test_busy_cursor.py` import it from there. `CLAUDE.md`'s tree and `architecture_explore.md` §5 name the new module.
- **Files**: `pybreeze/pybreeze_ui/busy_cursor.py` (new), `pybreeze/pybreeze_ui/menu/menu_utils.py`, `pybreeze/pybreeze_ui/menu/tools/tools_menu.py`, `pybreeze/pybreeze_ui/menu/automation_menu/automation_menu_factory.py`, `pybreeze/pybreeze_ui/menu/extend_jeditor_tab_menu/jupyter_lab_tab.py`, `test/test_utils/test_busy_cursor.py`, `CLAUDE.md`, `architecture_explore.md`
- **Checks**: ruff passes; the cursor, Tools-menu and automation menu tests pass (30).
- **Open items**: none.

## U-20260925-118 · 2026-09-25 · JSON Format, HAR Import and Response Inspector show the wait cursor while they work · #ux #tools

- **What**: three tools do all their work on the UI thread on whatever is pasted or opened. Measured (Python 3.11): a 3.3 MB JSON array took 1.2 s to lay out (`reformat_json`, numbers kept as written, keys sorted) and 2.1 s more to show (`setPlainText` of the 7.9 MB result; a `QPlainTextEdit` took the same, the time being the layout of 800,000 lines), and a 16.7 MB one about ten times that; a HAR export of 20,000 requests (8.5 MB) took seconds to parse and list. All that time the IDE sat still with nothing to say it was working. (A first measurement of 3.1 s for `reformat_json` was taken while other sessions loaded the machine and under a profiler; alone it is 1.1–1.2 s, of which the encoder with an indent, pure Python, is most.)
- **Change**: `JsonFormatGUI._run` (Format and Minify), `HarImportGUI.load_text` (Open and paste) and `ResponseInspectorGUI.analyze` do their work inside `busy_cursor()`.
- **Tests**: `test_busy_cursor.py`: each of the three runs its parsing with the wait cursor shown and leaves none behind.
- **Files**: `pybreeze/pybreeze_ui/tools_gui/json_format_gui.py`, `pybreeze/pybreeze_ui/tools_gui/har_import_gui.py`, `pybreeze/pybreeze_ui/tools_gui/response_inspector_gui.py`, `test/test_utils/test_busy_cursor.py`, `architecture_explore.md` (tool tabs, test count)
- **Checks**: ruff passes; the cursor, JSON Format, HAR Import and Response Inspector tests pass (67). `test/test_utils` collects 2724 tests in 151 files.
- **Open items**: none.

## U-20260925-119 · 2026-09-25 · Read the Docs' PDF of latest has its Chinese pages, in Traditional Chinese glyphs · #docs #verify

- **What**: the fix for the PDF build (xelatex with xeCJK and the Noto CJK TC fonts, in `docs/source/conf.py`) could only be checked once Read the Docs built it from `main`. PR #137 was merged at 11:41 UTC and build 34757403 of `latest` (6b52cc72) finished at 11:42.
- **Checks**: the PDF downloaded from `https://pybreeze.readthedocs.io/_/downloads/en/latest/pdf/` has 65 pages and 10,374 CJK characters (it had 52 pages and none before). The build log shows xeCJK loading `NotoSerifCJKTC`; its only font warnings are the italic and slanted CJK shapes the fonts do not have (upright used instead). The embedded fonts are named `NotoSerifCJKjp-Regular` and `NotoSansCJKjp-*`: the PostScript name of the `.ttc` collection the build image installs, not the face used. Rendered from the PDF and from `NotoSerifCJKtc-Regular.otf`, 說, 直, 過, 明, 數, 設 and 定 overlap by 0.90–0.97 of their ink, with 說 (whose Japanese form differs) no lower than characters with one form. Side by side, 說 is the Traditional Chinese shape.
- **Files**: none (this records a check)
- **Open items**: none.

## U-20260925-120 · 2026-09-25 · SonarCloud's last findings in the package, after main's analysis of the release · #refactor #sonarcloud

- **What**: after PR #137 was merged, SonarCloud analysed `main` (12:00 UTC): 127 open issues, down from 174, none of them bugs or vulnerabilities (there were one and three), quality gate passed. Eight are in the package, all but one kind cleared on `dev` since (U-20260925-112, not yet on `main`). That one is S5886 in `terminal_style._colour_parameter`: SonarCloud reads `dataclasses.replace` as returning a `DataclassInstance`, not the `TextStyle` it is given, and flags every `return replace(...)`, the split U-20260925-112 made included. The 119 in tests are composite assertions (91), several calls under `pytest.raises` (21) and a few others, most in tests written before this month.
- **Change** (refactor, no behaviour change): `_colour_change(number)` gives the attribute a basic colour parameter sets and its colour (or `None`), and `_colour_parameter` assigns `replace(style, **{attribute: colour})` and returns `style`, as `apply_sgr` already does with the switches and extended colours (not flagged). `_FOREGROUND` and `_BACKGROUND` name the two attributes where the literals were repeated.
- **Files**: `pybreeze/utils/terminal_style.py`
- **Checks**: ruff passes; the terminal-style, SSH terminal output and pure-logic fuzz tests pass (111). The published pybreeze 1.0.33 wheel, from the merge, carries `pybreeze/pybreeze_ui/editor_main/pybreeze_icon.ico` (U-20260925-105).
- **Open items**: none.

## U-20260925-121 · 2026-09-25 · The rest of SonarCloud's test findings on main: one condition per assertion, one call under pytest.raises, monkeypatch · #refactor #test #sonarcloud

- **What**: SonarCloud's analysis of `main` after the release listed 119 findings in tests. U-20260925-113 had already cleared those in this month's new tests; left were 77 assertions joining conditions with `and` in 44 files (S9073), 12 `pytest.raises` blocks that also ran setup (building a URL or a request, constructing a client wrapper, parsing the input) where the setup raising would pass the test (S5778), and five tests setting `FakeSmtp`'s class attributes by hand in `test_mail_report.py` (S8997; the fixture reset them on the next test's setup, so nothing leaked, but a failure left them set).
- **Change** (tests only): each flagged single-line composite assertion becomes one assertion per condition, found by SonarCloud's reported line and split through the parsed source (the message kept; none had a trailing comment), 158 assertions for the 77; the setup moves above each of the 12 `with pytest.raises`, with `_read(opener, url)` in `test_public_http.py` (the response now closed) and a local function for the undo-scope test, whose body is itself a `with` block; the five use `monkeypatch.setattr(FakeSmtp, ...)`. The two S3415 findings (argument order said to be mixed) could not be pinned to a line and are left.
- **Files**: 44 test files for the assertions, `test_url_convert.py`, `test_har_import.py`, `test_ssh_reentrancy.py`, `test_sftp_upload.py`, `test_sftp_session_sharing.py`, `test_curl_request_fidelity.py`, `test_script_templates.py`, `test_diagram_serialization.py`, `test_public_http.py`, `test_mail_report.py`
- **Checks**: ruff passes; the 44 files' tests pass (1130, 1 skipped), and the ten files of the other two changes (369).
- **Open items**: none.

## U-20260925-122 · 2026-09-25 · Assertions in the two diagram test files put the actual value first · #refactor #test #sonarcloud

- **What**: SonarCloud's last two test findings on `main` (S3415) said `test_diagram_serialization.py` and `test_diagram_editing.py` each mix "actual first" and "expected first". In the first, `assert {"DB", "B", "C", "Decision"} == texts` put the expected set first; in the second no comparison had a literal on its left, and the likely reading is the three whose left side is a tuple display, `(node.node_w, node.node_h) == (340, 80)` and the two `(x, y, w, h) == expected` of the resize tests.
- **Change** (tests only): the set comparison reads `texts == {...}`; the three tuples are named first (`size`, `geometry`) and compared with the expected value on the right.
- **Files**: `test/test_utils/test_diagram_serialization.py`, `test/test_utils/test_diagram_editing.py`
- **Checks**: ruff passes; the two files' 97 tests pass. The run of the full suite at 018c2cf had 2703 passed and six child-IDE tests timed out while other sessions loaded the machine (30 minutes against the usual 7 to 13); the three files they are in pass alone (11, the slowest 18 s).
- **Open items**: none.

## U-20260925-123 · 2026-09-25 · A test checks that the published package holds every module and data file · #test #packaging

- **What**: after the release, the pybreeze 1.0.33 wheel on PyPI was compared with the source tree: all 209 modules it had then are in it, and so is the window's icon, the one file under `pybreeze/` that is not Python (U-20260925-105). Nothing guarded it: `pyproject.toml` finds packages with `namespaces = false`, so a folder of modules added without an `__init__.py` would be left out of the wheel while every test (run from source) passed, and a data file not listed as package data would be missing from every installed IDE, as the icon was.
- **Change**: `test_packaging.py` (new): every folder under `pybreeze/` with modules has an `__init__.py`, and every file there that is not Python is listed under `[tool.setuptools.package-data]` in both `pyproject.toml` and `dev.toml`.
- **Files**: `test/test_utils/test_packaging.py` (new), `architecture_explore.md` (test count)
- **Checks**: both tests pass; with a folder holding one module and no `__init__.py` added under `pybreeze/`, the first fails naming it (then removed). `test/test_utils` collects 2726 tests in 152 files.
- **Open items**: none.

## U-20260925-124 · 2026-09-25 · A report mail gives up on a silent mail server after 30 s (progress #102) · #done #fix #mail

- **What**: `progress.md` #102: MailThunder's `SMTPWrapper(host, port)` passes no timeout to `smtplib.SMTP_SSL`, so a mail server that accepted the connection and then fell silent held the report-mail thread for good, and the run window never said whether the report went. It was left waiting for a `timeout` argument in MailThunder; je_mail_thunder 0.0.29, the latest, still has none (`smtp_wrapper.py`: `super().__init__(host, port)`).
- **Change**: no need to wait. Every SMTP client gets its socket from `_get_socket(host, port, timeout)`, which `SMTP_SSL` itself overrides (the same signature on Python 3.10, 3.11 and 3.14). `send_report` builds the client from `_with_timeout(SMTPWrapper)`, a subclass handing `_get_socket` `_SMTP_TIMEOUT_SECONDS` (30), so connecting, the TLS handshake and each reply have that long; a server that runs out of it ends the send as `sending failed (TimeoutError)` in the run window, through the `OSError` the send already catches. `architecture.md` §6 lists what the report mail relies on in `je_mail_thunder`.
- **Tests**: `TestAServerThatStopsAnswering` in `test_mail_report.py`: a local server that accepts and never speaks, reached through a real `SMTP_SSL` subclass shaped like `SMTPWrapper`, with the timeout set to 1 s: `send_report` returns `sending failed (TimeoutError)` well within 15 s. With the client built from `SMTPWrapper` directly, as before, it was still waiting after 15 s (nothing returned).
- **Files**: `pybreeze/extend/mail_thunder_extend/mail_thunder_setting.py`, `test/test_utils/test_mail_report.py`, `architecture.md` (§6), `architecture_explore.md` (report-mail thread, test count), `progress.md` (#102 removed)
- **Checks**: ruff passes; the file's 25 tests pass. `test/test_utils` collects 2727 tests in 152 files.
- **Open items**: none.

## U-20260925-125 · 2026-09-25 · The report-mail timeout is tested against MailThunder's own client; the packaging test reads the table · #test #mail #packaging

- **What**: a review of the unpushed commits found two gaps in their tests. The report-mail timeout (U-20260925-124) rests on MailThunder's `SMTPWrapper` being an `SMTP_SSL` that connects as it is built, through `_get_socket`, but its test used a stand-in, so a MailThunder that changed shape would lose the timeout with every test still passing. And the packaging test (U-20260925-123) looked for the exact text `"pkg" = ["name"]`, so a second file in the same list failed it, and it went through every file on disk under `pybreeze/`, a log or an editor's backup included.
- **Change** (tests only): `test_the_timeout_reaches_mailthunders_own_client` builds `_with_timeout(SMTPWrapper)` from the installed je_mail_thunder against the silent server (after checking it subclasses `SMTP_SSL`) and expects a `TimeoutError` within the patched second. The packaging test reads the `[tool.setuptools.package-data]` table into package -> names, and takes the non-Python files from `git ls-files` (all files when git cannot answer); the folder check still takes every module on disk, so a folder added a moment ago is caught before it is committed.
- **Also noted, not changed**: a mail server that falls silent after the greeting, during login, is now also given up after 30 s, but MailThunder's `later_init` catches that and the run window says the login failed; the connection's QUIT on the way out can take another 30 s. The hang it replaces had no end.
- **Files**: `test/test_utils/test_mail_report.py`, `test/test_utils/test_packaging.py`, `architecture_explore.md` (test count)
- **Checks**: both files pass (28); a folder with a module and no `__init__.py` still fails the folder check, and an untracked `stray.log` under `pybreeze/` no longer fails the data check (both then removed). `test/test_utils` collects 2728 tests in 152 files.
- **Open items**: none.

## U-20260925-126 · 2026-09-25 · The three tools take the wait cursor as a decorator; Response Inspector reads its input once · #refactor #tools

- **What**: from the same review: U-20260925-118 put the whole bodies of `JsonFormatGUI._run`, `HarImportGUI.load_text` and `ResponseInspectorGUI.analyze` under `with busy_cursor():`, re-indenting some 60 lines and adding a level of nesting where a context manager made by `@contextmanager` also works as a decorator. `analyze` read the whole text box twice through `exact_text()` (stripped, then as typed), inside the part called slow for a few megabytes. And the CoT Code Review Tab and Dock entries still spelled out the name U-20260925-112 made a constant, where the prompt editors' entries beside them add " Tab" to theirs.
- **Change** (refactor, no behaviour change): `@busy_cursor()` on the three methods, whose bodies are as they were before U-20260925-118; `analyze` reads the box once and strips that copy; `_COT_CODE_REVIEW + " Tab"` / `" Dock"` (and `分頁` / `停駐窗格`) in the two language files.
- **Files**: `pybreeze/pybreeze_ui/tools_gui/json_format_gui.py`, `har_import_gui.py`, `response_inspector_gui.py`, `pybreeze/extend_multi_language/extend_english.py`, `extend_traditional_chinese.py`
- **Checks**: ruff passes; the cursor, JSON Format, HAR Import, Response Inspector and language-parity tests pass (93); the four labels read as before.
- **Open items**: none.

## U-20260925-127 · 2026-09-25 · The wait cursor shows before the work starts; HAR Import's file read is under it too · #ux #tools

- **What**: from the same review: `busy_cursor()` set the override cursor and went straight into the work. Windows changes the cursor at once, but X11, Wayland and macOS apply a cursor change as their event loop runs, so there it could show only once the work was over (not seen here: this machine and CI run Windows). And HAR Import's Open read the file (`read_text_capped`, up to its size cap, possibly from a slow drive) before `load_text`, outside the cursor.
- **Change**: `busy_cursor()` handles the events waiting once, user input excluded (`QEventLoop.ProcessEventsFlag.ExcludeUserInputEvents`), after setting the cursor and before the block; `HarImportGUI.open_file` reads the file under it.
- **Tests**: `test_busy_cursor.py`: a zero-delay timer set before the block has fired inside it.
- **Files**: `pybreeze/pybreeze_ui/busy_cursor.py`, `pybreeze/pybreeze_ui/tools_gui/har_import_gui.py`, `test/test_utils/test_busy_cursor.py`, `architecture_explore.md` (test count)
- **Checks**: ruff passes; the cursor, HAR Import, Tools-menu, automation menu and JupyterLab lifecycle tests pass (71). `test/test_utils` collects 2729 tests in 152 files.
- **Open items**: none.

## U-20260925-128 · 2026-09-25 · The published 1.0.33 starts from its installed files, with its icon · #verify #packaging

- **What**: every test runs the IDE from the source tree; the release CI made from PR #137 (pybreeze 1.0.33 on PyPI) had not been started from what `pip` installs.
- **Checks**: the 1.0.33 wheel installed with `pip install --no-deps --target` into a folder of its own, its dependencies taken from this repository's `.venv`, started from an empty working folder: `pybreeze` imported from the installed folder, `open_main_window()` built the window with its icon (`pybreeze_icon.ico` read from the installed package) and the title PyBreeze, and the application closed cleanly. A full `pip install pybreeze==1.0.33` into a fresh venv was tried too and stopped: PyPI served PySide6-Addons (168 MB) too slowly to finish in the hour allowed; resolving the dependencies is what CI does from `requirements.txt` on every run.
- **Files**: none (this records a check)
- **Open items**: none.

## U-20260925-129 · 2026-09-25 · CI caches pip's downloads for the unit-test legs · #ci

- **What**: every unit-test leg downloaded its whole dependency set again, PySide6 and its add-ons, JupyterLab and the automation packages among them: "Install dependencies" took 143–192 s of each leg's roughly ten minutes on the release run of PR #137 (run 36130816490).
- **Change**: `actions/setup-python` in both workflows' `unit-tests` job gets `cache: pip`, keyed on `requirements.txt` (and `dev_requirements.txt` in `dev.yml`). Only the downloaded files are cached: pip still resolves every version from PyPI on each run, so the unpinned dependencies are tested at their newest, as before. The publish job is unchanged. `CLAUDE.md` (Branching & CI) and `architecture_explore.md` §18 say so.
- **Files**: `.github/workflows/dev.yml`, `.github/workflows/stable.yml`, `CLAUDE.md`, `architecture_explore.md`
- **Checks**: actionlint 1.7.12 passes both workflows (it passed them before too). The saving shows only once CI runs them; the first run on each Python fills the cache.
- **Open items**: none.

## U-20260925-130 · 2026-09-25 · The JupyterLab tab passes its empty token under jupyter_server 2's names too · #fix #jupyter

- **What**: the tab starts its server with no token and no password through `--ServerApp.token=` and `--ServerApp.password=`, jupyter_server 1.x's names. Started with the tab's flags on jupyter_server 2.17 (this `.venv`), the server logs `ServerApp.token config is deprecated in 2.0. Use IdentityProvider.token.`; a release that stops reading the old name would make a token of its own, and the tab would load a login page.
- **Change**: the server is also given `--IdentityProvider.token=` and `--PasswordIdentityProvider.hashed_password=`. The old two stay, for a jupyter_server 1.x in an older environment. `CLAUDE.md` (Security › JupyterLab) and `architecture_explore.md` name both.
- **Tests**: `test_jupyter_lifecycle.py`: the server's command line carries all four, empty.
- **Files**: `pybreeze/pybreeze_ui/jupyter_lab_gui/jupyter_lab_thread.py`, `test/test_utils/test_jupyter_lifecycle.py`, `CLAUDE.md`, `architecture_explore.md` (JupyterLab tab, test count)
- **Checks**: started for real on jupyter_server 2.17 / JupyterLab 4.5.6 with only the new names, and with all four: `/api/status` answered 200 without a token each time, and the log has no error (with the old names, the one deprecation warning). The JupyterLab lifecycle and readiness tests pass (29). `test/test_utils` collects 2730 tests in 152 files.
- **Open items**: none.

## U-20260925-131 · 2026-09-25 · The SSH terminal's error stream, a failed send, Disconnect and the shell's opening are tested · #test #ssh

- **What**: the SSH and SFTP tests (247) left 10 % of `ssh_command_widget.py` untested, among it paths a user takes: the reader forwarding what the shell writes to its error stream, a command whose send fails, the Disconnect button, and `open_shell_channel` (keepalive, pty size, non-blocking channel).
- **Change**: `TestWhatTheOtherPathsDo` in `test_ssh_terminal_output.py`: error-stream output reaches the terminal like the rest; a send that raises (`Socket is closed`) says so in the terminal and leaves the typed command in the line; Disconnect closes the channel and the client, forgets both, shows the disconnected status and signals the change once; the shell is opened after setting the transport's keepalive, as an `xterm` of the size asked, and made non-blocking.
- **Files**: `test/test_utils/test_ssh_terminal_output.py`, `architecture_explore.md` (test count)
- **Checks**: the four tests pass as the code stands; the file's 54 pass. `test/test_utils` collects 2734 tests in 152 files.
- **Open items**: none.

## U-20260925-132 · 2026-09-25 · The SFTP tree's connect: missing input and a root it cannot list are tested · #test #ssh

- **What**: in the same coverage run, `ssh_file_viewer_widget.py` (the SFTP tree) was at 91 %; untested were two things a user meets on connecting: Connect with no host or no user, and a session that comes up but cannot list `/`.
- **Change**: `TestConnecting` in `test_sftp_tree_actions.py`: a blank host (spaces only) with a user asks for the missing input and starts no connect thread; when listing the root fails after the connect (`Permission denied`), the failure is reported as a failed connect naming the reason, and the state change is signalled once.
- **Files**: `test/test_utils/test_sftp_tree_actions.py`, `architecture_explore.md` (test count)
- **Checks**: both pass as the code stands; the file's 41 pass. `test/test_utils` collects 2736 tests in 152 files.
- **Open items**: none.

## U-20260925-133 · 2026-09-25 · The diagram editor's New asks only about unsaved changes, as Open and Close do · #fix #diagram

- **What**: New asked "Discard current diagram?" whenever anything was on the canvas, a diagram just saved or just opened included, while Open and closing the tab, the dock or the IDE ask only when there are changes that are not saved (`_may_discard_edits`, the undo stack not clean). The two diagram tests on the untested part of `diagram_editor_widget.py` found it.
- **Change**: `_new_diagram` goes through `_may_discard_edits` with its own question ("The diagram has changes that are not saved. Start a new one and lose them?" / 架構圖還有沒存的變更。要新建一個並捨棄它們嗎？), keeping the key `diagram_editor_confirm_new`; `diagram_editor_confirm_title`, used by nothing else, is gone from both dictionaries, which now carry 759 keys each (the three READMEs and `architecture_explore.md` say so). The Tools page, in English and Chinese, says New asks when there are changes not saved.
- **Tests**: `test_unsaved_on_close.py`: after a save, New clears the canvas and the path without a question (it asked once before the change); with an unsaved change it asks, keeps the diagram on No, and on Yes clears it with a clean undo history.
- **Files**: `pybreeze/pybreeze_ui/diagram_editor/diagram_editor_widget.py`, `pybreeze/extend_multi_language/extend_english.py`, `extend_traditional_chinese.py`, `test/test_utils/test_unsaved_on_close.py`, `README.md`, `README/README_zh-TW.md`, `README/README_zh-CN.md`, `docs/source/Eng/menu_tools.rst`, `docs/source/Zh/menu_tools.rst`, `architecture_explore.md` (key count, test count)
- **Checks**: ruff passes; the unsaved-changes, language-parity and diagram editor tests pass (58) and the docs markup test (26). `test/test_utils` collects 2738 tests in 152 files.
- **Open items**: none.

## U-20260925-134 · 2026-09-25 · A test keeps unused keys out of PyBreeze's dictionaries · #test #i18n

- **What**: U-20260925-133 found `diagram_editor_confirm_title` left in both dictionaries after the only code asking for it had gone. A sweep of all 759 keys then: 623 are named in the package outside the language files, and the other 136 all belong to families whose names are built as the IDE runs (`error_text_` + a tag, `run_window_` + a notice, `header_finding_` + a code, `http_status_category_` + a class, the SFTP tree's menu, headers and types, and JEditor's Plugin Browser's `plugin_browser_*`); within each family every suffix is one the code produces.
- **Change**: `test_translation_keys_used.py` (new) fails on a key of `pybreeze_english_word_dict` that the package does not name in quotes and that belongs to none of those families (listed in the test with where each is built). The Traditional Chinese dictionary has the same keys (`test_language_parity.py`).
- **Files**: `test/test_utils/test_translation_keys_used.py` (new), `architecture_explore.md` (test count)
- **Checks**: it passes; with a key nothing asks for added to the English dictionary, it fails naming it (then removed). `test/test_utils` collects 2739 tests in 153 files.
- **Open items**: none.

## U-20260925-135 · 2026-09-25 · The diagram editor's Mermaid import without nodes, and with text it cannot read, is tested · #test #diagram

- **What**: two branches of `DiagramEditorWidget._import_mermaid` had no test: pasted text with no node in it, and text the parser cannot read.
- **Change**: `TestMermaidImport` in `test_diagram_editor_widget.py`: `flowchart TD` with only a comment says there was nothing to import and leaves the diagram on the canvas as it was; a parser error (stubbed as `ValueError("unexpected <b>token</b>")`) is shown with its markup escaped, as text, and leaves the canvas as it was too.
- **Files**: `test/test_utils/test_diagram_editor_widget.py`, `architecture_explore.md` (test count)
- **Checks**: both pass as the code stands; the file's 19 pass. `test/test_utils` collects 2741 tests in 153 files.
- **Open items**: none.

## U-20260926-01 · 2026-09-26 · Running a folder: the entry itself, a file that cannot start, and a folder that cannot be read are tested · #test #run

- **What**: `process_executor_utils.py` was at 87 % in the executor tests. The folder run of each automation menu (Run ▸ all the files of a folder) was tested from `run_one_after_another` on, but not the entry that asks for the files (`run_dir_files_with_package`), nor a file whose run cannot start, which never ends and so must hand over to the next one from the event loop.
- **Change**: `TestRunningAFolder` in `test_run_output.py`: when no file's run can start, all three files are still tried, in order; the entry passes the files picked to the batch and runs nothing when nothing was picked; an error while asking (`OSError` from the folder) is logged under the package's name, not raised out of the menu.
- **Files**: `test/test_utils/test_run_output.py`, `architecture_explore.md` (test count)
- **Checks**: the three pass as the code stands; the class's five pass. `test/test_utils` collects 2744 tests in 153 files.
- **Open items**: none.

## U-20260926-02 · 2026-09-26 · A long script that is not JSON, and a run whose done hook fails, are tested · #test #run

- **What**: two branches of `TaskProcessManager` had no test: a script too long for the command line that is not JSON (`_write_script_file` writes it as it is, so the package reports what is wrong with it), and a done hook that raises (the report mail's), which is logged and must still let the run window end its run.
- **Change**: `test_long_scripts.py`: a non-JSON script of 50,000 characters is written to the file unchanged and the file is removed after; a run whose hook raises `RuntimeError` gives its output, logs the error, and the run window's Stop button is disabled again (`run_ended()`). The shared `_run` helper now takes the hook and checks `run_ended()` for every run.
- **Files**: `test/test_utils/test_long_scripts.py`, `architecture_explore.md` (test count)
- **Checks**: the file's four pass. With the hook's `except Exception` narrowed to `ZeroDivisionError` (in a separate worktree), the hook test fails on the Stop button still enabled. `test/test_utils` collects 2746 tests in 153 files.
- **Open items**: none.

## U-20260926-03 · 2026-09-26 · The second test that every translation key is asked for is removed · #test #i18n

- **What**: `test_translation_keys_used.py` (U-20260925-134) checks what `test_language_keys_used.py` already checked since 2026-09-24: a key of `pybreeze_english_word_dict` that the package does not name in quotes and that belongs to no family built at run time. Both list the same families.
- **Change**: `test_translation_keys_used.py` is deleted; `test_language_keys_used.py` stays.
- **Files**: `test/test_utils/test_translation_keys_used.py` (deleted), `architecture_explore.md` (test count)
- **Checks**: `test_language_keys_used.py` passes. `test/test_utils` collects 2745 tests in 152 files.
- **Open items**: none.

## U-20260926-04 · 2026-09-26 · The SFTP connect is tested against a real SSH server on the loopback address, on paramiko 4 and 5 · #test #ssh #security

- **What**: paramiko 5.0.0 is on PyPI (May 2026, after a security audit: SHA-1 signatures and key exchanges and GSSAPI removed, the Diffie-Hellman group exchange minimum raised to 2048 bits, `PKey.from_path`'s `passphrase` renamed `password`). `requirements.txt` and `pyproject.toml` do not pin paramiko, so a new install and CI take 5.0. Every SSH test stubbed the network; nothing logged in for real.
- **Change**: `test_ssh_loopback.py` (new) runs a paramiko server on 127.0.0.1 on its own thread, with an SFTP subsystem that lists one entry, and connects through `SFTPClientWrapper.connect()` as the file tree does (host key policy with the question answered yes, known hosts under `tmp_path`): with a password, and with an encrypted OpenSSH RSA key, an Ed25519 key, a PEM ECDSA key and an encrypted PKCS#8 RSA key; the RSA host key is signed with rsa-sha2, the question is asked once over two connects and the host is kept, a wrong password raises `AuthenticationException` and leaves no session, and a server whose RSA host key signs only with SHA-1 is refused without a question. The code needed no change: PyBreeze's key loader calls `from_private_key_file(path, passphrase)` positionally, and naming SHA-1 algorithms paramiko 5 no longer has in `disabled_algorithms` is harmless (it filters by name).
- **Files**: `test/test_utils/test_ssh_loopback.py` (new), `architecture_explore.md` (tests)
- **Checks**: the 8 tests pass on paramiko 4.0.0 (the venv) and on 5.0.0 (installed to a separate folder and put first on `PYTHONPATH`); the 281 SSH and SFTP tests pass on 5.0.0 too. With `sftp_session.SHA1_ALGORITHMS` emptied at run time, the SHA-1 server test fails (the connect succeeds). `test/test_utils` collects 2753 tests in 153 files.
- **Sources**: [paramiko changelog](https://www.paramiko.org/changelog.html), [oss-sec: security audit of Paramiko, fixes in 5.0](https://seclists.org/oss-sec/2026/q2/414)
- **Open items**: none.

## U-20260926-05 · 2026-09-26 · Each automation menu's Run entries are followed to the run they start · #test #menu #run

- **What**: the five automation packages' Run entries (the script, with the report sent, a folder, a folder with the report sent) each go through a module of their own under `extend/process_executor/<package>/`, and MailThunder's one entry through another. The modules were at 67 % (80 % for MailThunder): no test triggered an entry, so a module naming the wrong package, or an entry that sends the report when it should not, went unseen.
- **Change**: `test_automation_run_entries.py` (new) builds each package's menu on a stand-in main window, with `build_process` and `run_dir_files_with_package` recorded in the package's executor module, and triggers every Run entry in order: APITestka, AutoControl, FileAutomation, LoadDensity and WebRunner run their own package as a script and as a folder, without and then with the report sent; MailThunder runs its script without one.
- **Files**: `test/test_utils/test_automation_run_entries.py` (new), `architecture_explore.md` (test count)
- **Checks**: the 6 pass. In a separate worktree, with WebRunner's "with send" entry passing `False` and LoadDensity's package renamed `je_api_testka`, those two fail and the others pass. `test/test_utils` collects 2759 tests in 154 files.
- **Open items**: none.

## U-20260926-06 · 2026-09-26 · Refactor: the CoT global rules lose the RAG rules section nothing filled in · #refactor #ai

- **What**: `build_global_rule_template(rag_rules=None, prompt="")` could add an "8. RAG Rules" section to the global rules every CoT review step is wrapped in, but its only caller (`cot_chain.py`) never passes `rag_rules`, so the branch never ran (the module was at 50 % coverage). Had it run, a list of rules would have come out on one line: they were joined with `""`.
- **Change**: refactor, no change in what is sent. The parameter, the branch and the `{rag_rules_section}` placeholder are gone; the function is `build_global_rule_template(prompt: str = "") -> str` with a docstring, and the template keeps the empty line the empty section left, so the text is the same.
- **Files**: `pybreeze/pybreeze_ui/extend_ai_gui/prompt_edit_gui/cot_code_review_prompt_templates/global_rule.py`
- **Checks**: the SHA-256 of the output for three prompts (empty, one with braces, multi-line Chinese) is the same before and after; `test_cot_chain.py` and `test_prompt_store.py` pass (68); ruff is clean.
- **Open items**: none.

## U-20260926-07 · 2026-09-26 · The prompt editor's Create, and Save with no file shown, are tested · #test #ai

- **What**: `prompt_editor_widget.py` was at 87 %. Create was tested only when it asks before replacing typed text; it had no test that it writes the built-in template, starts watching the new file and shows it, nor that it leaves an existing file alone. Save and Create with no file shown (as after a prompt file that cannot be read) and a change on disk to a file other than the one shown had none either.
- **Change**: `test_prompt_store.py`: `TestCreatingAPromptFile` (the file gets the built-in text, the edit area shows it unmodified, the watcher has the path, one message; an existing file keeps its text and the message names it) and `TestWithNoFileShown` (Save warns and writes nothing; Create does nothing; a change to another file leaves typed text as it was).
- **Files**: `test/test_utils/test_prompt_store.py`, `architecture_explore.md` (test count)
- **Checks**: the five pass as the code stands; the file's 48 pass. `test/test_utils` collects 2764 tests in 154 files.
- **Open items**: none.

## U-20260926-08 · 2026-09-26 · The log file's rotation when a process opens it is tested · #test #logging

- **What**: every run appends to `~/.pybreeze/logs/PyBreeze.log`, and a process opening it moves a file past `PYBREEZE_LOG_MAX_BYTES` (100 MB by default) to `PyBreeze.log.1` first. No test covered it: the move itself (`os.replace`), the fallback for a size that is not a number, and the warning when the move is refused never ran in the suite.
- **Change**: `TestRotatingOnOpen` in `test_logger.py`: a file past the size is moved aside, replacing an older `.1`, and the new record starts a new file; a file within the size is appended to; a size of 0 turns rotation off; `100MB` is not a number and gives the default; a refused move (`PermissionError`, as Windows gives while another process holds the file) warns `not rotated` and the record is still appended.
- **Files**: `test/test_utils/test_logger.py`, `architecture_explore.md` (test count)
- **Checks**: the five pass as the code stands; the file's 19 pass. `test/test_utils` collects 2769 tests in 154 files.
- **Open items**: none.

## U-20260926-09 · 2026-09-26 · The READMEs and the guide say where the log is and how to move or cap it · #docs #logging

- **What**: nothing a user reads said where PyBreeze logs (`~/.pybreeze/logs/PyBreeze.log`), nor that `PYBREEZE_LOG_FILE` names another file and `PYBREEZE_LOG_MAX_BYTES` sets the size past which the file is renamed with `.1` appended when a process first writes to it. Only `architecture_explore.md` did.
- **Change**: a "Log file" subsection under Quick Start in `README.md`, `README/README_zh-TW.md` and `README/README_zh-CN.md`, and a "The Log File" section at the end of the Getting Started page in the English and Chinese guides: the path, UTF-8, appended to by every run with the process ID on each line, only warnings and errors in the Code result tab, and the two variables (default 104857600 bytes, `0` never renames).
- **Files**: `README.md`, `README/README_zh-TW.md`, `README/README_zh-CN.md`, `docs/source/Eng/getting_started.rst`, `docs/source/Zh/getting_started.rst`
- **Checks**: what it says was checked against `utils/logging/logger.py` (the format string carries `%(process)d`; the rename is `os.replace` to `<name>.1` in `_open`, on the first record) and `code_result_logs.py`; `test_docs_markup.py` passes (26).
- **Open items**: none.

## U-20260926-10 · 2026-09-26 · JSON Format's failures while writing the result are tested · #test #json

- **What**: `json_process.py` was at 89 %. What the tests covered was parsing; the other side was not: `reformat_json` given a value already parsed (laid out as it is), or one JSON cannot hold, and a `RecursionError` while writing the result. That last one is not only in theory: on Python 3.14 a list nested from about 15,550 to 16,950 deep parses and then cannot be written, indented or minified (measured; 3.11 and 3.13 fail both at the same depth, 1,000 and 3,000).
- **Change**: `TestWhatCannotBeWrittenBack` in `test_json_process.py`: a dict is laid out indented by four with sorted keys; a set raises `ITEJsonException`; with the writer raising `RecursionError`, Reformat and Minify both raise `ITEJsonException` (the writer is stubbed, since the depth that reaches it depends on the Python version).
- **Files**: `test/test_utils/test_json_process.py`, `architecture_explore.md` (test count)
- **Checks**: the four pass as the code stands; the file's 33 pass. `test/test_utils` collects 2773 tests in 154 files.
- **Open items**: none.

## U-20260926-11 · 2026-09-26 · The POSIX side of stopping a run's process tree is tested on any platform · #test #run

- **What**: `subprocess_util.py` stops a run's whole tree with `taskkill /T /F` on Windows and SIGTERM to the child's process group elsewhere. CI runs on Windows only, so the POSIX branches (`os.killpg`, and the `0` creation flags) never ran in it.
- **Change**: `TestOnPosix` in `test_subprocess_util.py` stands in for `sys` in the module with `platform = "linux"`: the child gets no window flag and `start_new_session`; `stop_tree` sends SIGTERM to the child's process group (pid 4242), runs no `taskkill`, and leaves `terminate()` alone once the group is gone.
- **Files**: `test/test_utils/test_subprocess_util.py`, `architecture_explore.md` (test count)
- **Checks**: both pass on Windows; the file's 15 pass. `test/test_utils` collects 2775 tests in 154 files.
- **Open items**: none.

## U-20260926-12 · 2026-09-26 · The JupyterLab launcher's port check, output tail and stop without terminate are tested · #test #jupyter

- **What**: `jupyter_lab_thread.py` was at 89 %. The tests stubbed `_port_open`, so the real check never ran, and nothing covered waiting with no server started, the end of the server's output when there is none or it cannot be read, or `stop()` when `terminate()` is refused.
- **Change**: in `test_jupyter_ready.py`: waiting with no process raises `RuntimeError` ("not started"); `TestIsThePortOpen` against a real listening socket on 127.0.0.1 and the same port once closed; `TestWhatTheServerWrote` (no output: empty; a closed file: empty, not an exception; a long output: only its end); and `stop()` with a process whose `terminate()` raises `PermissionError` still forgets the process and closes the output file.
- **Files**: `test/test_utils/test_jupyter_ready.py`, `architecture_explore.md` (test count)
- **Checks**: the seven pass as the code stands; the file's 25 pass. `test/test_utils` collects 2782 tests in 154 files.
- **Open items**: none.

## U-20260926-13 · 2026-09-26 · prthinker's menu message and its install from a picked source folder are tested · #test #prthinker

- **What**: `build_prthinker_menu.py` was at 88 %: every test stood in for `_tell`, so the message box it shows was never built, and a saved file the review would not take (Review current file on a file `review_current_file` refuses) said nothing in the tests. `build_automation_install_menu.py` (93 %) had no test for a correct source folder picked on the first install: that it is saved and installed.
- **Change**: `test_prthinker_menu_review.py`: the saved-but-refused file tells `prthinker_need_saved_file_message`; the real `_tell` shows a message box parented to the window, titled with the menu's name, with the key's text, deleted on close. `test_install_menu.py`: a picked folder with prthinker's `pyproject.toml` is added to the settings (the other settings kept) and pip installs it.
- **Files**: `test/test_utils/test_prthinker_menu_review.py`, `test/test_utils/test_install_menu.py`, `architecture_explore.md` (test count)
- **Checks**: the three pass as the code stands; the two files' 18 pass. `test/test_utils` collects 2785 tests in 154 files.
- **Open items**: none.

## U-20260926-14 · 2026-09-26 · The main window's own answers to a tab or dock that keeps its work are tested · #test #ui

- **What**: `main_ui.py` was at 83 %. `may_close()` and `AskingDock` had tests, but not what the main window does with their answers: a tab close the user refuses, an IDE close refused by one tab, and marking the docks as asked (so they do not ask twice) only when everything agreed.
- **Change**: `TestTheMainWindow` in `test_unsaved_on_close.py`, calling `PyBreezeMainWindow`'s methods on a stand-in window with tabs and asking docks that answer as told: a refused `close_tab` leaves the tab; `closeEvent` with one tab saying no is ignored and closes nothing; `_tool_tabs_may_close` marks the docks `already_asked` when all agree and not otherwise, and asks a docked widget too. The file's two identical "widget that answers" stand-ins are now one (`_Answering`).
- **Files**: `test/test_utils/test_unsaved_on_close.py`, `architecture_explore.md` (test count)
- **Checks**: the four pass as the code stands; the file's 19 pass. `test/test_utils` collects 2789 tests in 154 files.
- **Open items**: none.

## U-20260926-15 · 2026-09-26 · A theme given at launch is tested to survive saved settings that fail to apply · #test #ui

- **What**: `_apply_given_theme` (`main_ui.py`) makes the theme given to `start_editor(theme=...)` the picked one and applies the saved settings with it; when those raise (the saved settings and the files they reopen can be anything), it logs the error and applies the theme alone. The start tests run in a child IDE with good settings, so that branch never ran.
- **Change**: `test_start_theme.py`: with a window whose `startup_setting()` raises `KeyError`, the picked theme becomes the one given, `apply_stylesheet` is called once with it, and the error is logged (the settings dict entry is restored after the test).
- **Files**: `test/test_utils/test_start_theme.py`, `architecture_explore.md` (test count)
- **Checks**: it passes as the code stands; the file's 8 pass. `test/test_utils` collects 2790 tests in 154 files.
- **Open items**: none.

## U-20260926-16 · 2026-09-26 · CI actions pinned by commit on their Node 24 releases; only the publish job keeps its token; Dependabot tracks actions after a week · #ci #security

- **What**: GitHub removed Node 20 from its runners on 2026-09-23, and `dev.yml` and `stable.yml` still named the Node 20 majors (`actions/checkout@v4`, `setup-python@v5`, `upload-artifact@v4`, `download-artifact@v4`) by movable tags: 14 `uses:` in all, including in the job that holds the PyPI token and `contents: write`. Every checkout left the job token in `.git/config` (5, zizmor's `artipacked`), and `dependabot.yml` had neither a `github-actions` entry nor a `cooldown`. Workspace item X-21 lists this for PyBreeze, done in ten other repositories with a `test_workflow_actions.py` (APITestka's is the reference); it is also the SHA half of `progress.md` #54.
- **Change**: each action is pinned to the commit of its current release with the version as a comment: `actions/checkout` v7.0.1, `actions/setup-python` v7.0.0, `actions/upload-artifact` v7.0.1, `actions/download-artifact` v8.0.1 (a digest mismatch now fails the download), `SonarSource/sonarqube-scan-action` v8.2.2. The release notes were read for what changed on the way: Node 24 throughout, setup-python v7 drops the `pip-install` input (not used here), download-artifact v5 changes paths only for downloads by ID (here: by name). Every checkout says `persist-credentials: false`, except `publish`'s (`true`: it pushes the version bump and its tag). `dependabot.yml` gains a weekly `github-actions` entry on `dev`, and both entries wait 7 days before proposing a release (`cooldown`). `test_workflow_actions.py` (new, after APITestka's) fails on a `uses:` without a 40-hex commit and a version comment, one action at two commits, a Dependabot entry not on `dev` or without the week, a checkout that does not decide on `persist-credentials`, or a kept token outside `stable.yml`. CLAUDE.md states the rule; `progress.md` #54 keeps only trusted publishing and prthinker installed from the head of its `main`.
- **Files**: `.github/workflows/dev.yml`, `.github/workflows/stable.yml`, `.github/dependabot.yml`, `test/test_utils/test_workflow_actions.py` (new), `CLAUDE.md`, `architecture_explore.md` (CI, test count), `progress.md` (#54)
- **Checks**: actionlint is clean; the 9 new tests pass, and 6 of them fail on the previous workflows. `uvx zizmor --offline .github`: 14 high and 6 medium before, none after; 6 informational remain, as in APITestka (trusted publishing, and the computed version expanded in `run:` blocks of `publish`). The commits were read from the GitHub API for each tag. The workflows run on GitHub only, so the first `dev` push shows the rest. `test/test_utils` collects 2799 tests in 155 files.
- **Sources**: [Node 20 is no longer available in GitHub Actions](https://github.blog/changelog/2026-09-23-node-20-is-no-longer-available-in-github-actions/), [Deprecation of Node 20 on GitHub Actions runners](https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/), [actions/setup-python releases](https://github.com/actions/setup-python/releases)
- **Open items**: none here; the workspace's X-21 line for PyBreeze can go once this reaches `main`.

## U-20260926-17 · 2026-09-26 · A test keeps requirements.txt, pyproject.toml and dev.toml asking for the same packages · #test #packaging

- **What**: `pyproject.toml` is what users install, `dev.toml` must keep the same `dependencies` (CLAUDE.md), and `requirements.txt` (read by `dev_requirements.txt`) is what CI installs and says it is kept in step with both. Nothing checked any of it: a pin moved in one file alone would leave CI testing another Qt than the release ships. JEditor and APITestka have a `test_requirement_pins.py` for this (workspace X-23).
- **Change**: `test_requirement_pins.py` (new): `dev.toml`'s dependencies equal `pyproject.toml`'s; `requirements.txt` lists exactly `pyproject.toml`'s dependencies; nothing in `dev_requirements.txt` asks for a package the package pins with another specifier. Names compare as pip compares them (PEP 503, spaces in the specifier ignored), and a test says so. It needs `tomllib`, so it skips on the 3.10 leg. CLAUDE.md names the test in its `dev.toml` rule.
- **Files**: `test/test_utils/test_requirement_pins.py` (new), `CLAUDE.md`, `architecture_explore.md` (test count)
- **Checks**: the four pass; in a separate worktree, with PySide6 at 6.11.1 in `requirements.txt` and `requests` added to `dev.toml`, the two file checks fail. `test/test_utils` collects 2803 tests in 156 files.
- **Open items**: none.

## U-20260926-18 · 2026-09-26 · Refactor: the last typing.List, typing.Dict and quoted annotations give way to the built-in forms · #refactor

- **What**: CLAUDE.md asks for Python 3.10 annotations, but seven modules still used `typing.List` / `typing.Dict`, `typing.Callable` and quoted class names under `from __future__ import annotations`. Ruff 0.16 (July 2026), whose default rule set grew from 59 to 413 rules, reports them as UP006, UP035 and UP037 (28 findings); ruff 0.15.8, in `.venv`, does not.
- **Change**: refactor, no change in behaviour: `dict[...]` and `list[...]` for `Dict` and `List` (`prthinker_setting.py`, `prthinker_setting_dialog.py`, `prthinker_process.py`), `Callable` from `collections.abc` (`python_task_process_manager.py`, `automation_menu_factory.py`, `output_actions.py`, placed in sorted order), and `SFTPClientWrapper` unquoted in two annotations of `sftp_session.py`.
- **Files**: the seven modules above
- **Checks**: `ruff check pybreeze/ test/` (0.15.8) is clean, and ruff 0.16.9 finds no UP006/UP035/UP037 in `pybreeze/`; the 150 tests of the prthinker, menu, run, SFTP and tool modules pass.
- **Open items**: the rest of what ruff 0.16 reports is `progress.md` #123.

## U-20260926-19 · 2026-09-26 · Snapping a diagram node or image to the grid is tested · #test #diagram

- **What**: `DiagramNode.itemChange` and `DiagramImage.itemChange` round a moved item's position to the grid when the grid is on, the grid has a size and the item is not being resized. No test moved an item with the grid on; the tests only checked that the scene hands the grid settings to both classes.
- **Change**: `test_diagram_grid_snap.py` (new), for a node and an image each, with the grid on at 20 for the test: a move to (33, 47) lands on (40, 40) and one to (-9, 11) on (0, 20); with the grid off, or of size 0, or while the item is resized, it stays where it was put. They guard the behaviour before the two copies of the snapping code become one.
- **Files**: `test/test_utils/test_diagram_grid_snap.py` (new), `architecture_explore.md` (test count)
- **Checks**: the 8 pass as the code stands. `test/test_utils` collects 2811 tests in 157 files.
- **Open items**: none.

## U-20260926-20 · 2026-09-26 · Refactor: one grid-snapping check for diagram nodes and images · #refactor #diagram

- **What**: `DiagramNode.itemChange` and `DiagramImage.itemChange` (`diagram_items.py`) each held their own three nested `if`s and rounding for snapping a move to the grid, the same logic written twice. Ruff 0.16 reports the nesting as SIM102.
- **Change**: refactor, no change in behaviour: `_snaps_to_grid(kind, resizing)` (the grid is on and sized, and the item is not being resized, read from the class as before) and `_on_grid(position, grid_size)` (the nearest grid point), both module functions with docstrings; each `itemChange` returns `_on_grid(...)` when a position change `_snaps_to_grid(...)`.
- **Files**: `pybreeze/pybreeze_ui/diagram_editor/diagram_items.py`
- **Checks**: `test_diagram_grid_snap.py` (U-20260926-19) passes before and after; 141 diagram tests pass; ruff 0.15.8 is clean and ruff 0.16.9 finds no SIM102 in the file.
- **Open items**: none.

## U-20260926-21 · 2026-09-26 · The regex tester's group-nesting scan and a worker that cannot start or dies part-way are tested · #test #regex

- **What**: `regex_tester.py` was at 92 %. `_group_nesting_too_deep` refuses a pattern nested past 100 groups before `re` recurses into it, and skips escaped parentheses and those inside a character class; no test had a character class in it, so a pattern such as `[(((…]` being refused would have gone unseen. A worker process that cannot start, or whose output stops mid-way (killed, out of memory), had no test either.
- **Change**: `test_regex_tester.py`: 101 `(` inside `[...]`, or escaped, are not groups, and the class pattern compiles and matches `(`; after a class closes, groups count again; `find_matches_bounded` raises `RegexTesterException` when starting the worker raises `OSError`, and when its output is cut-off JSON.
- **Files**: `test/test_utils/test_regex_tester.py`, `architecture_explore.md` (test count)
- **Checks**: the four pass as the code stands; the file's 36 pass. `test/test_utils` collects 2815 tests in 157 files.
- **Open items**: none.

## U-20260926-22 · 2026-09-26 · The AI panels' response cap is tested against compressed bombs, and CVE-2026-44432 found not to reach it · #test #security #network

- **What**: CVE-2026-44432: urllib3 2.6.x with Brotli installed decodes all the rest of a Brotli body on the second `HTTPResponse.read(amt)`, and in `drain_conn()`; fixed in 2.7.0. `.venv` has urllib3 2.6.3 with brotli 1.2.0, and PyBreeze does not declare urllib3 (`progress.md` #53), so an environment may keep an affected one. The three AI panels read an endpoint's answer through `read_capped_text` (`utils/network/http_client.py`), which streams it under a 16 MB cap; a bomb decoded whole would pass that cap in memory. The diagram editor's image download goes through urllib, which does not decode bodies.
- **Change**: measured first, on urllib3 2.6.3 and brotli 1.2.0, with a loopback server: a Brotli body of 632 bytes decoding to 400 MB, and one of 1.6 MB spread over many reads, are both refused at the cap with a peak working set of 66 and 69 MB, so the chunked read does not reach the flaw, and nothing in the package changes. `TestACompressedBomb` in `test_http_client.py` keeps it so: for gzip, deflate and br (skipped without brotli), a body decoding to 128 MB is refused at a 1 MB cap with less than 32 MB traced at the peak.
- **Files**: `test/test_utils/test_http_client.py`, `architecture_explore.md` (test count)
- **Checks**: the three pass (1.5 s at most each); the file's 35 pass. Read whole, the body would need 128 MB. `test/test_utils` collects 2818 tests in 157 files.
- **Sources**: [CVE-2026-44432](https://www.sentinelone.com/vulnerability-database/cve-2026-44432/), [urllib3 security advisories](https://github.com/urllib3/urllib3/security)
- **Open items**: none; declaring urllib3 at all stays with #53.

## U-20260926-23 · 2026-09-26 · A run with no interpreter found, a tick after the run, and the POSIX venv lookup are tested · #test #run

- **What**: `python_task_process_manager.py` still had untested branches: `start_test_process` and `start_test_process_file` when `renew_path()` finds no Python (it has already said so in the run window), `pull_text` ticking after the run is gone, and `find_venv_path` on POSIX (`venv/bin`, `.venv/bin`); CI runs on Windows only.
- **Change**: `test_task_manager_venv.py`: with `renew_path` answering no, neither start creates a process (`subprocess.Popen` fails the test if called) or starts the timer; a tick with no process stops the timer. `test_venv_path.py`: with `sys` in the module standing in for Linux, a `.venv/bin` in the working folder is found.
- **Files**: `test/test_utils/test_task_manager_venv.py`, `test/test_utils/test_venv_path.py`, `architecture_explore.md` (test count)
- **Checks**: the four pass as the code stands; the two files' 11 pass. `test/test_utils` collects 2822 tests in 157 files.
- **Open items**: none.

## U-20260926-24 · 2026-09-26 · The SSH terminal is tested against a real shell channel on the loopback server · #test #ssh

- **What**: every SSH terminal test stubbed the channel, so nothing checked the tab end to end against a server: the pty request and its size, a command reaching the shell and its output coming back through the reader thread, Ctrl+C, and a shell the server ends. The loopback server of U-20260926-04 had no shell.
- **Change**: `test_ssh_loopback.py`: the loopback server answers pty, shell and window-change requests and runs a small shell on each channel (greets, answers each line with `got: <line>`, ends on `exit`), recording what it was asked. `TestTheTerminal` logs `SSHCommandWidget` in with a password through its own Connect (host key asked through the stand-in asker): the pty is `xterm` and the last size the server was told is the one the widget holds, at least 20 by 5; `echo 中文` is sent, the line is cleared, and `got: echo 中文` is shown; Interrupt sends `\x03`; `exit` ends the shell and the session with it (`bye` shown, no client left); Disconnect closes the shell.
- **Files**: `test/test_utils/test_ssh_loopback.py`, `architecture_explore.md` (tests)
- **Checks**: the file's 13 pass on paramiko 4.0.0 and on 5.0.0; the five terminal tests passed three more runs in a row (13 to 16 s each). `test/test_utils` collects 2827 tests in 157 files.
- **Open items**: none.

## U-20260926-25 · 2026-09-26 · Refactor: the loopback SSH server moves to a helper module, with a folder on disk for SFTP · #refactor #test #ssh

- **What**: the loopback SSH server lived inside `test_ssh_loopback.py`, and its SFTP listed one made-up entry. Testing the SFTP tree's actions end to end needs a server whose SFTP works on real files, from another test file.
- **Change**: test code only, the tests unchanged. `test/test_utils/ssh_loopback_server.py` (new, not a test file, like `started_window.py`) holds `LoopbackServer`, `USER`, `PASSWORD` and `ONE_ENTRY`; `LoopbackServer(sftp_root=...)` serves a folder on disk as `/` (list, stat, lstat, open for reading and writing, remove, rename, POSIX rename, mkdir, rmdir), and without one lists the one entry as before. `test_ssh_loopback.py` imports it.
- **Files**: `test/test_utils/ssh_loopback_server.py` (new), `test/test_utils/test_ssh_loopback.py`
- **Checks**: the 13 tests of `test_ssh_loopback.py` pass; ruff is clean.
- **Open items**: none.

## U-20260926-26 · 2026-09-26 · The SFTP file tree is tested end to end against a real folder over SFTP · #test #ssh #sftp

- **What**: `ssh_file_viewer_widget.py` was at 92 %, and every file tree test stood in for the session: nothing followed an action through paramiko's SFTP to a file on disk.
- **Change**: `test_sftp_tree_loopback.py` (new): the loopback server serves a temporary folder (`docs/readme.txt`, `hello.txt`) as `/`, and `SSHFileTreeManager` logs in through its own Connect with the host key accepted by the stand-in asker. The root lists folders first, with `docs` waiting for its first expand, which lists it; a new folder, a rename and a delete end on disk and in the tree; deleting a folder that is not empty says why and leaves it; a download is written whole with no temporary file left beside it; an upload lands in the folder and the folder is listed again; an upload over an existing file asks first, and No keeps the server's copy while Yes replaces it. The host-key stand-in and the event-waiting loop the two loopback test files share are now `accept_every_host_key` and `wait_until` in `ssh_loopback_server.py`.
- **Files**: `test/test_utils/test_sftp_tree_loopback.py` (new), `test/test_utils/ssh_loopback_server.py`, `test/test_utils/test_ssh_loopback.py`, `architecture_explore.md` (tests)
- **Checks**: the 10 pass on paramiko 4.0.0 and 5.0.0, and in two more runs (11 to 12 s); `test_ssh_loopback.py`'s 13 still pass. `test/test_utils` collects 2837 tests in 158 files.
- **Open items**: none.

## U-20260926-27 · 2026-09-26 · Adding a shape or a text box with a click on the diagram canvas is tested · #test #diagram

- **What**: `diagram_scene.py` was at 91 %, and the main way to draw was among what no test reached: a click on the canvas with a shape tool (rectangle, rounded rectangle, ellipse, diamond) or the text tool picked. A right press in those modes, and the dashed line following the mouse while a connection is drawn, had no test either.
- **Change**: `test_diagram_scene_actions.py`: `TestAddingByClicking`: for each shape tool a click adds one node of that shape centred on the click, the tool goes back to Select, and one undo removes it; the text tool adds a text box with the translated "Text", centred likewise; a right press in a shape mode adds nothing and keeps the tool. With a connection started from a node, a mouse move takes the dashed line's end to the pointer, its start staying at the node's centre.
- **Files**: `test/test_utils/test_diagram_scene_actions.py`, `architecture_explore.md` (test count)
- **Checks**: the seven pass as the code stands; the file's 18 pass. `test/test_utils` collects 2844 tests in 158 files.
- **Open items**: none.

## U-20260926-28 · 2026-09-26 · The diagram canvas's Paste entry, and the commands that have nothing to act on, are tested · #test #diagram

- **What**: in `diagram_scene.py`, the Paste entry a right click on the empty canvas offers once something is copied had no test, nor did a paste with nothing copied, Bring to Front or Send to Back with no other item on the canvas, or align right, top, bottom and horizontal centre with fewer than two nodes selected (only align left and vertical centre were tested).
- **Change**: `test_diagram_editing.py`: a copied node is pasted from the empty canvas's menu, and with nothing copied the menu has no Paste; a node alone keeps its stacking order and leaves no undo step, both ways; a paste with nothing copied adds nothing and no undo step. `test_diagram_align.py`: all six align commands, and both distributes, change nothing and leave no undo step with too few nodes.
- **Files**: `test/test_utils/test_diagram_editing.py`, `test/test_utils/test_diagram_align.py`, `architecture_explore.md` (test count)
- **Checks**: the nine new cases pass as the code stands; the two files' 59 pass. `test/test_utils` collects 2853 tests in 158 files.
- **Open items**: none.

## U-20260926-29 · 2026-09-26 · Skill Send's missing input, a second send, its answer, an unknown template and a redirect's port are tested · #test #ai

- **What**: `skills_send_gui.py` was at 93 %: no test sent with the URL or the prompt missing, sent again while a request was running, showed an answer, picked a template name the panel does not know, or named a redirect whose port is not a number.
- **Change**: `test_skills_panel.py`: with no URL, or a prompt of spaces, the panel asks for them and sends nothing; a send while a request runs is ignored and the running one kept; an answer is shown and Send enabled again; an unknown template name leaves the prompt as it was. `test_skills_request.py`: a redirect to `host:notaport` names the scheme and host only, one to `host:8443` names the port too, and neither shows the query's token.
- **Files**: `test/test_utils/test_skills_panel.py`, `test/test_utils/test_skills_request.py`, `architecture_explore.md` (test count)
- **Checks**: the seven pass as the code stands; the two files' 21 pass. `test/test_utils` collects 2860 tests in 158 files.
- **Open items**: none.

## U-20260926-30 · 2026-09-26 · The project tree menu's helpers are tested where the file system says no · #test #file-tree

- **What**: `file_tree_context_menu.py` was at 94 %; what its helpers do when the file system refuses had no test: an index that is no item, a view with no root, two paths that cannot be compared, a path that cannot be looked at, a link `unlink` refuses (a junction), a name whose place cannot be resolved, a new folder whose name leaves the folder, and Reveal with nothing under the cursor.
- **Change**: `TestWhatTheHelpersDoWhenTheFileSystemSaysNo` in `test_file_tree_context_menu.py`: no path for an invalid index; the working folder for a view with no root; not the same file when `samefile` fails; no link when `lstat` fails; `rmdir` when `unlink` raises `IsADirectoryError`; a name refused, with one warning, when `resolve` raises `OSError`; New Folder with `../outside` makes nothing and warns; Reveal with no path starts no file manager.
- **Files**: `test/test_utils/test_file_tree_context_menu.py`, `architecture_explore.md` (test count)
- **Checks**: the eight pass as the code stands; the file's 82 pass. `test/test_utils` collects 2868 tests in 158 files.
- **Open items**: none.

## U-20260926-31 · 2026-09-26 · A week date, a missing translation, dir(pybreeze), undo merging by key and a bare -F field are tested · #test

- **What**: small branches the coverage report still listed: the timestamp converter's fallback to `datetime.fromisoformat` for ISO forms its own pattern does not take, `error_text` for a language without the entry, `pybreeze.__dir__`, `DiagramSnapshotCommand.mergeWith` with a step on another property (Qt asks only commands of the same `id()`, and two keys can share a CRC), and a curl `-F` fragment without `=`.
- **Change**: `test_timestamp_converter.py`: `2026-W39-6T12:00` reads as 2026-09-26 12:00 UTC from Python 3.11, and is refused before it (each skipped on the other versions). `test_error_text.py`: with no entry for it the tool's English stays. `test_package_facade.py`: `dir(pybreeze)` lists every name in `__all__`. `test_diagram_undo_fidelity.py`: a step does not merge with one on another property, merges with one on the same (taking its result), and a step with no key has id -1. `test_curl_request_fidelity.py`: `-F just-a-name` is left out of the form.
- **Files**: `test/test_utils/test_timestamp_converter.py`, `test/test_utils/test_error_text.py`, `test/test_utils/test_package_facade.py`, `test/test_utils/test_diagram_undo_fidelity.py`, `test/test_utils/test_curl_request_fidelity.py`, `architecture_explore.md` (test count)
- **Checks**: the six new tests pass as the code stands (the pre-3.11 one skipped on 3.11); the five files' 179 pass. `test/test_utils` collects 2874 tests in 158 files.
- **Open items**: none.

## U-20260926-32 · 2026-09-26 · An automation run from the editor tab in front, and start_process, are tested · #test #run

- **What**: `build_process` (`process_executor_utils.py`) takes the script from the editor tab in front and names the run after its file. The tests covered a script given outright and a tab that is no editor, but not the usual case, an editor tab in front; and every test stood in for `start_process`, so its own two lines never ran.
- **Change**: `test_build_task_process.py`: with an editor tab in front holding a script and `D:/scripts/login_test.json`, `build_process(..., send_mail=True)` starts the run with the tab's text, the report flag, the default buffer and `login_test.json` as its subject; `start_process` builds the run with the report flag and buffer and hands it the package, the script and the subject.
- **Files**: `test/test_utils/test_build_task_process.py`, `architecture_explore.md` (test count)
- **Checks**: both pass as the code stands; the file's 18 pass. `test/test_utils` collects 2876 tests in 158 files.
- **Open items**: none.

## U-20260926-33 · 2026-09-26 · The output reader facing a queue nobody empties, and an escape cut off by the end, are tested · #test #run

- **What**: `queue_pump.py`'s reader waits for room when its queue is full, checking between waits whether it should still read; no test filled the queue, so a reader that waited for ever once the run window was gone would have gone unseen. The end of the output, where an escape sequence held for its rest is passed on as it is, had no test either.
- **Change**: `test_queue_pump.py`: with a queue of one already full and `keep_reading` turning false after two waits, the reader thread ends within the join and adds nothing; `done \x1b[3` at the very end of the output comes out whole.
- **Files**: `test/test_utils/test_queue_pump.py`, `architecture_explore.md` (test count)
- **Checks**: both pass as the code stands; the file's 25 pass. `test/test_utils` collects 2878 tests in 158 files.
- **Open items**: none.

## U-20260926-34 · 2026-09-26 · let_run_out is tested with a signal whose disconnect raises · #test #threads

- **What**: `thread_keeper.let_run_out` disconnects a closing widget's worker signals and keeps the thread until it ends. PySide6 6.11 warns when a signal has nothing connected; older versions raised, and the function logs that and goes on. No test made a disconnect raise, so a change that let the error stop the rest (the remaining signals still connected, the thread not kept) would have gone unseen.
- **Change**: `test_ai_gui_lifecycle.py`: with the first signal's `disconnect()` raising `RuntimeError`, the next signal is still disconnected, the thread is kept, and the refusal is logged at debug level.
- **Files**: `test/test_utils/test_ai_gui_lifecycle.py`, `architecture_explore.md` (test count)
- **Checks**: it passes as the code stands; the file's 12 pass. `test/test_utils` collects 2879 tests in 158 files.
- **Open items**: none.

## U-20260926-35 · 2026-09-26 · An SFTP connect with a key file that cannot be loaded is tested · #test #ssh #sftp

- **What**: `SFTPClientWrapper._log_in` raises `ValueError` with the reason the key did not load (`unloadable_key_reason`) before any authentication; the loopback tests logged in with good keys only, and the line had no test.
- **Change**: `test_ssh_loopback.py`: a text file given as the key, with no passphrase, is refused with the translated "unsupported key" message, and the wrapper is left unconnected.
- **Files**: `test/test_utils/test_ssh_loopback.py`, `architecture_explore.md` (test count)
- **Checks**: it passes as the code stands; the file's 14 pass. `test/test_utils` collects 2880 tests in 158 files.
- **Open items**: none.

## U-20260926-36 · 2026-09-26 · A plugin entry that cannot be built, a JWT's dates in the Response Inspector, and CoT's skip and re-submit are tested · #test #plugins #ai

- **What**: branches of the coverage report with no test: `set_plugin_menu` leaving out a plugin whose entry raises while building it; the Response Inspector writing a token's timestamp claims (`exp`, `iat`, ...) as dates, and `open_jwt_in_decoder` before anything was analysed; the CoT review skipping a name that is not a step of the chain; and a second Start while a review runs.
- **Change**: `test_plugin_menu.py`: an entry raising `KeyError` is left out and logged, and the plugin after it still gets its entry. `test_response_inspector_gui.py`: a token with `exp` 1790380800 shows `exp: 2026-09-26…` under the decoded payload; with nothing analysed, opening the JWT decoder does nothing. `test_cot_session_reuse.py`: with `not_a_step.md` before `linter.md`, one request goes out and one answer is shown; Start while a review runs starts no second one and keeps the running one.
- **Files**: `test/test_utils/test_plugin_menu.py`, `test/test_utils/test_response_inspector_gui.py`, `test/test_utils/test_cot_session_reuse.py`, `architecture_explore.md` (test count)
- **Checks**: the new cases pass as the code stands; the three files' 85 pass. `test/test_utils` collects 2884 tests in 158 files.
- **Open items**: none.

## U-20260926-37 · 2026-09-26 · The diagram editor's grid switches, Save with no file yet, and adding an image from a file are tested · #test #diagram

- **What**: `diagram_editor_widget.py` was at 93 %: the Show Grid and Snap to Grid switches, Save on a diagram never saved (which must ask where), and adding an image from a file, a picture or a file that is none, had no test.
- **Change**: `test_diagram_editor_widget.py`: Show Grid turns the view's grid on and off, and Snap turns the scene's snapping on and off for nodes and images alike (their class settings put back after the test); Save with no file yet goes to Save As; a 40 by 30 PNG picked in the dialog is added with its path as the source; a text file named `.png` adds nothing and shows one warning.
- **Files**: `test/test_utils/test_diagram_editor_widget.py`, `architecture_explore.md` (test count)
- **Checks**: the four pass as the code stands; the file's 23 pass. `test/test_utils` collects 2888 tests in 158 files.
- **Open items**: none.

## U-20260926-38 · 2026-09-26 · start_editor's order of steps is tested by running it, not by reading its source · #test #gc

- **What**: the test that the IDE collects garbage on its GUI thread only checked that `start_editor`'s source text contained `collect_garbage_on_gui_thread(new_ide)`: a call moved after the window was built, or never reached, still passed. `start_editor` itself runs only in child IDEs that leave through `os._exit`, which coverage cannot follow, so it was never measured either.
- **Change**: `test_gui_thread_gc.py`: the test runs `start_editor(debug_mode=True, theme="dark_teal.xml")` with the collector switch, the window, the event loop and `os._exit` recorded, and checks their order: collection moves to the application's thread first, then the window is opened with the options given, then the event loop runs, and the process ends with its return code (3 here).
- **Files**: `test/test_utils/test_gui_thread_gc.py`
- **Checks**: it passes as the code stands; the file's 8 pass. The test count is unchanged (2888).
- **Open items**: none.

## U-20260926-39 · 2026-09-26 · Two tests that read PyBreeze's own source now check what the code does · #test

- **What**: two tests passed or failed on words in the source rather than on behaviour. `test_logger.py` checked that the logger module's text had neither `root.setLevel` nor `basicConfig`, so `logging.getLogger().setLevel(...)` would have passed. `test_unsaved_on_close.py` checked that `add_dock`'s text contained `AskingDock()`, so a dock built that way and then never attached would have passed too. (The JEditor contract tests read JEditor's source on purpose: they guard its internals.)
- **Change**: the logger test imports `pybreeze.utils.logging.logger` in a fresh interpreter whose root logger is set to ERROR and checks that its level and handlers are unchanged. The dock test calls `add_dock(window, "Hash")` and checks that the one dock with a widget is an `AskingDock` in the window's right dock area.
- **Files**: `test/test_utils/test_logger.py`, `test/test_utils/test_unsaved_on_close.py`
- **Checks**: both pass as the code stands (the files' 19 and 19); in a separate worktree, with `logging.getLogger().setLevel(logging.DEBUG)` added to the logger module, the logger test fails. The test count is unchanged (2888).
- **Open items**: none.

## U-20260926-40 · 2026-09-26 · Each diagram node shape is tested by rendering the canvas and reading its pixels · #test #diagram

- **What**: a rounded rectangle node is drawn by its own `paint` (`_RoundedRectBody` in `diagram_items.py`); no test rendered a node, so a body drawn square, or not at all, would have gone unseen, and the ellipse body had never been made in a test before U-20260926-27.
- **Change**: `test_diagram_shapes_drawn.py` (new) renders one node of each shape, filled red, into a white 140 by 60 image at 1:1: the middle is filled for every shape, and a pixel three in from the top left corner is filled for the rectangle only, not for the rounded rectangle, the ellipse or the diamond.
- **Files**: `test/test_utils/test_diagram_shapes_drawn.py` (new), `architecture_explore.md` (test count)
- **Checks**: the 8 pass as the code stands; in a separate worktree, with the rounded rectangle's `paint` drawing a plain rectangle, its corner test fails. `test/test_utils` collects 2896 tests in 159 files.
- **Open items**: none.
