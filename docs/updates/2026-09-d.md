# 2026-09 update log, continued a third time

Continues [2026-09-c.md](2026-09-c.md), which passed the batch size limit. Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260925-116 · 2026-09-25 · Start the 2026-09-d batch · #docs

- **What**: `2026-09-c.md` had reached 970 lines, past the batch size limit of about 800 (Batch rules, item 2). Its 115 entries, the batch table's count and the index rows agree, as they do for `2026-09.md` (218) and `2026-09-b.md` (120).
- **Change**: new entries go into `2026-09-d.md`, listed in the batch table.
- **Files**: `docs/updates/2026-09-d.md`, `docs/updates/README.md`
- **Open items**: none.

## U-20260925-117 · 2026-09-25 · busy_cursor moves out of the menus into pybreeze_ui · #refactor

- **What**: `busy_cursor()` was written into `menu/menu_utils.py` for the menu entries (U-20260925-114), and the tool tabs are to use it next for what they do with a large input; a tool tab importing from the menus' helpers would run against how the layers depend on each other.
- **Change** (refactor, no behaviour change): it lives in `pybreeze/pybreeze_ui/busy_cursor.py`, beside the other UI helpers (`plain_text.py`, `closing.py`); `menu_utils.py`, the Tools menu, the automation menu factory, the JupyterLab tab entry and `test_busy_cursor.py` import it from there. `CLAUDE.md`'s tree and `architecture_explore.md` §5 name the new module.
- **Files**: `pybreeze/pybreeze_ui/busy_cursor.py` (new), `pybreeze/pybreeze_ui/menu/menu_utils.py`, `pybreeze/pybreeze_ui/menu/tools/tools_menu.py`, `pybreeze/pybreeze_ui/menu/automation_menu/automation_menu_factory.py`, `pybreeze/pybreeze_ui/menu/extend_jeditor_tab_menu/jupyter_lab_tab.py`, `test/test_utils/test_busy_cursor.py`, `CLAUDE.md`, `architecture_explore.md`
- **Checks**: ruff passes; the cursor, Tools-menu and automation menu tests pass (30).
- **Open items**: none.

## U-20260925-118 · 2026-09-25 · JSON Format, HAR Import and Response Inspector show the wait cursor while they work · #ux #tools

- **What**: three tools do all their work on the UI thread on whatever is pasted or opened. Measured (Python 3.11): a 3.3 MB JSON array took 1.2 s to lay out (`reformat_json`, numbers kept as written, keys sorted) and 2.1 s more to show (`setPlainText` of the 7.9 MB result; a `QPlainTextEdit` took the same, the time being the layout of 800,000 lines), and a 16.7 MB one about ten times that; a HAR export of 20,000 requests (8.5 MB) took seconds to parse and list. All that time the IDE sat still with nothing to say it was working. (A first measurement of 3.1 s for `reformat_json` was taken while other sessions loaded the machine and under a profiler; alone it is 1.1–1.2 s, of which the encoder with an indent, pure Python, is most.)
- **Change**: `JsonFormatGUI._run` (Format and Minify), `HarImportGUI.load_text` (Open and paste) and `ResponseInspectorGUI.analyze` do their work inside `busy_cursor()`.
- **Tests**: `test_busy_cursor.py`: each of the three runs its parsing with the wait cursor shown and leaves none behind.
- **Files**: `pybreeze/pybreeze_ui/tools_gui/json_format_gui.py`, `pybreeze/pybreeze_ui/tools_gui/har_import_gui.py`, `pybreeze/pybreeze_ui/tools_gui/response_inspector_gui.py`, `test/test_utils/test_busy_cursor.py`, `architecture_explore.md` (tool tabs, test count)
- **Checks**: ruff passes; the cursor, JSON Format, HAR Import and Response Inspector tests pass (67). `test/test_utils` collects 2724 tests in 151 files.
- **Open items**: none.

## U-20260925-119 · 2026-09-25 · Read the Docs' PDF of latest has its Chinese pages, in Traditional Chinese glyphs · #docs #verify

- **What**: the fix for the PDF build (xelatex with xeCJK and the Noto CJK TC fonts, in `docs/source/conf.py`) could only be checked once Read the Docs built it from `main`. PR #137 was merged at 11:41 UTC and build 34757403 of `latest` (6b52cc72) finished at 11:42.
- **Checks**: the PDF downloaded from `https://pybreeze.readthedocs.io/_/downloads/en/latest/pdf/` has 65 pages and 10,374 CJK characters (it had 52 pages and none before). The build log shows xeCJK loading `NotoSerifCJKTC`; its only font warnings are the italic and slanted CJK shapes the fonts do not have (upright used instead). The embedded fonts are named `NotoSerifCJKjp-Regular` and `NotoSansCJKjp-*`: the PostScript name of the `.ttc` collection the build image installs, not the face used. Rendered from the PDF and from `NotoSerifCJKtc-Regular.otf`, 說, 直, 過, 明, 數, 設 and 定 overlap by 0.90–0.97 of their ink, with 說 (whose Japanese form differs) no lower than characters with one form. Side by side, 說 is the Traditional Chinese shape.
- **Files**: none (this records a check)
- **Open items**: none.

## U-20260925-120 · 2026-09-25 · SonarCloud's last findings in the package, after main's analysis of the release · #refactor #sonarcloud

- **What**: after PR #137 was merged, SonarCloud analysed `main` (12:00 UTC): 127 open issues, down from 174, none of them bugs or vulnerabilities (there were one and three), quality gate passed. Eight are in the package, all but one kind cleared on `dev` since (U-20260925-112, not yet on `main`). That one is S5886 in `terminal_style._colour_parameter`: SonarCloud reads `dataclasses.replace` as returning a `DataclassInstance`, not the `TextStyle` it is given, and flags every `return replace(...)`, the split U-20260925-112 made included. The 119 in tests are composite assertions (91), several calls under `pytest.raises` (21) and a few others, most in tests written before this month.
- **Change** (refactor, no behaviour change): `_colour_change(number)` gives the attribute a basic colour parameter sets and its colour (or `None`), and `_colour_parameter` assigns `replace(style, **{attribute: colour})` and returns `style`, as `apply_sgr` already does with the switches and extended colours (not flagged). `_FOREGROUND` and `_BACKGROUND` name the two attributes where the literals were repeated.
- **Files**: `pybreeze/utils/terminal_style.py`
- **Checks**: ruff passes; the terminal-style, SSH terminal output and pure-logic fuzz tests pass (111). The published pybreeze 1.0.33 wheel, from the merge, carries `pybreeze/pybreeze_ui/editor_main/pybreeze_icon.ico` (U-20260925-105).
- **Open items**: none.

## U-20260925-121 · 2026-09-25 · The rest of SonarCloud's test findings on main: one condition per assertion, one call under pytest.raises, monkeypatch · #refactor #test #sonarcloud

- **What**: SonarCloud's analysis of `main` after the release listed 119 findings in tests. U-20260925-113 had already cleared those in this month's new tests; left were 77 assertions joining conditions with `and` in 44 files (S9073), 12 `pytest.raises` blocks that also ran setup (building a URL or a request, constructing a client wrapper, parsing the input) where the setup raising would pass the test (S5778), and five tests setting `FakeSmtp`'s class attributes by hand in `test_mail_report.py` (S8997; the fixture reset them on the next test's setup, so nothing leaked, but a failure left them set).
- **Change** (tests only): each flagged single-line composite assertion becomes one assertion per condition, found by SonarCloud's reported line and split through the parsed source (the message kept; none had a trailing comment), 158 assertions for the 77; the setup moves above each of the 12 `with pytest.raises`, with `_read(opener, url)` in `test_public_http.py` (the response now closed) and a local function for the undo-scope test, whose body is itself a `with` block; the five use `monkeypatch.setattr(FakeSmtp, ...)`. The two S3415 findings (argument order said to be mixed) could not be pinned to a line and are left.
- **Files**: 44 test files for the assertions, `test_url_convert.py`, `test_har_import.py`, `test_ssh_reentrancy.py`, `test_sftp_upload.py`, `test_sftp_session_sharing.py`, `test_curl_request_fidelity.py`, `test_script_templates.py`, `test_diagram_serialization.py`, `test_public_http.py`, `test_mail_report.py`
- **Checks**: ruff passes; the 44 files' tests pass (1130, 1 skipped), and the ten files of the other two changes (369).
- **Open items**: none.

## U-20260925-122 · 2026-09-25 · Assertions in the two diagram test files put the actual value first · #refactor #test #sonarcloud

- **What**: SonarCloud's last two test findings on `main` (S3415) said `test_diagram_serialization.py` and `test_diagram_editing.py` each mix "actual first" and "expected first". In the first, `assert {"DB", "B", "C", "Decision"} == texts` put the expected set first; in the second no comparison had a literal on its left, and the likely reading is the three whose left side is a tuple display, `(node.node_w, node.node_h) == (340, 80)` and the two `(x, y, w, h) == expected` of the resize tests.
- **Change** (tests only): the set comparison reads `texts == {...}`; the three tuples are named first (`size`, `geometry`) and compared with the expected value on the right.
- **Files**: `test/test_utils/test_diagram_serialization.py`, `test/test_utils/test_diagram_editing.py`
- **Checks**: ruff passes; the two files' 97 tests pass. The run of the full suite at 018c2cf had 2703 passed and six child-IDE tests timed out while other sessions loaded the machine (30 minutes against the usual 7 to 13); the three files they are in pass alone (11, the slowest 18 s).
- **Open items**: none.

## U-20260925-123 · 2026-09-25 · A test checks that the published package holds every module and data file · #test #packaging

- **What**: after the release, the pybreeze 1.0.33 wheel on PyPI was compared with the source tree: all 209 modules it had then are in it, and so is the window's icon, the one file under `pybreeze/` that is not Python (U-20260925-105). Nothing guarded it: `pyproject.toml` finds packages with `namespaces = false`, so a folder of modules added without an `__init__.py` would be left out of the wheel while every test (run from source) passed, and a data file not listed as package data would be missing from every installed IDE, as the icon was.
- **Change**: `test_packaging.py` (new): every folder under `pybreeze/` with modules has an `__init__.py`, and every file there that is not Python is listed under `[tool.setuptools.package-data]` in both `pyproject.toml` and `dev.toml`.
- **Files**: `test/test_utils/test_packaging.py` (new), `architecture_explore.md` (test count)
- **Checks**: both tests pass; with a folder holding one module and no `__init__.py` added under `pybreeze/`, the first fails naming it (then removed). `test/test_utils` collects 2726 tests in 152 files.
- **Open items**: none.

## U-20260925-124 · 2026-09-25 · A report mail gives up on a silent mail server after 30 s (progress #102) · #done #fix #mail

- **What**: `progress.md` #102: MailThunder's `SMTPWrapper(host, port)` passes no timeout to `smtplib.SMTP_SSL`, so a mail server that accepted the connection and then fell silent held the report-mail thread for good, and the run window never said whether the report went. It was left waiting for a `timeout` argument in MailThunder; je_mail_thunder 0.0.29, the latest, still has none (`smtp_wrapper.py`: `super().__init__(host, port)`).
- **Change**: no need to wait. Every SMTP client gets its socket from `_get_socket(host, port, timeout)`, which `SMTP_SSL` itself overrides (the same signature on Python 3.10, 3.11 and 3.14). `send_report` builds the client from `_with_timeout(SMTPWrapper)`, a subclass handing `_get_socket` `_SMTP_TIMEOUT_SECONDS` (30), so connecting, the TLS handshake and each reply have that long; a server that runs out of it ends the send as `sending failed (TimeoutError)` in the run window, through the `OSError` the send already catches. `architecture.md` §6 lists what the report mail relies on in `je_mail_thunder`.
- **Tests**: `TestAServerThatStopsAnswering` in `test_mail_report.py`: a local server that accepts and never speaks, reached through a real `SMTP_SSL` subclass shaped like `SMTPWrapper`, with the timeout set to 1 s: `send_report` returns `sending failed (TimeoutError)` well within 15 s. With the client built from `SMTPWrapper` directly, as before, it was still waiting after 15 s (nothing returned).
- **Files**: `pybreeze/extend/mail_thunder_extend/mail_thunder_setting.py`, `test/test_utils/test_mail_report.py`, `architecture.md` (§6), `architecture_explore.md` (report-mail thread, test count), `progress.md` (#102 removed)
- **Checks**: ruff passes; the file's 25 tests pass. `test/test_utils` collects 2727 tests in 152 files.
- **Open items**: none.

## U-20260925-125 · 2026-09-25 · The report-mail timeout is tested against MailThunder's own client; the packaging test reads the table · #test #mail #packaging

- **What**: a review of the unpushed commits found two gaps in their tests. The report-mail timeout (U-20260925-124) rests on MailThunder's `SMTPWrapper` being an `SMTP_SSL` that connects as it is built, through `_get_socket`, but its test used a stand-in, so a MailThunder that changed shape would lose the timeout with every test still passing. And the packaging test (U-20260925-123) looked for the exact text `"pkg" = ["name"]`, so a second file in the same list failed it, and it went through every file on disk under `pybreeze/`, a log or an editor's backup included.
- **Change** (tests only): `test_the_timeout_reaches_mailthunders_own_client` builds `_with_timeout(SMTPWrapper)` from the installed je_mail_thunder against the silent server (after checking it subclasses `SMTP_SSL`) and expects a `TimeoutError` within the patched second. The packaging test reads the `[tool.setuptools.package-data]` table into package -> names, and takes the non-Python files from `git ls-files` (all files when git cannot answer); the folder check still takes every module on disk, so a folder added a moment ago is caught before it is committed.
- **Also noted, not changed**: a mail server that falls silent after the greeting, during login, is now also given up after 30 s, but MailThunder's `later_init` catches that and the run window says the login failed; the connection's QUIT on the way out can take another 30 s. The hang it replaces had no end.
- **Files**: `test/test_utils/test_mail_report.py`, `test/test_utils/test_packaging.py`, `architecture_explore.md` (test count)
- **Checks**: both files pass (28); a folder with a module and no `__init__.py` still fails the folder check, and an untracked `stray.log` under `pybreeze/` no longer fails the data check (both then removed). `test/test_utils` collects 2728 tests in 152 files.
- **Open items**: none.

## U-20260925-126 · 2026-09-25 · The three tools take the wait cursor as a decorator; Response Inspector reads its input once · #refactor #tools

- **What**: from the same review: U-20260925-118 put the whole bodies of `JsonFormatGUI._run`, `HarImportGUI.load_text` and `ResponseInspectorGUI.analyze` under `with busy_cursor():`, re-indenting some 60 lines and adding a level of nesting where a context manager made by `@contextmanager` also works as a decorator. `analyze` read the whole text box twice through `exact_text()` (stripped, then as typed), inside the part called slow for a few megabytes. And the CoT Code Review Tab and Dock entries still spelled out the name U-20260925-112 made a constant, where the prompt editors' entries beside them add " Tab" to theirs.
- **Change** (refactor, no behaviour change): `@busy_cursor()` on the three methods, whose bodies are as they were before U-20260925-118; `analyze` reads the box once and strips that copy; `_COT_CODE_REVIEW + " Tab"` / `" Dock"` (and `分頁` / `停駐窗格`) in the two language files.
- **Files**: `pybreeze/pybreeze_ui/tools_gui/json_format_gui.py`, `har_import_gui.py`, `response_inspector_gui.py`, `pybreeze/extend_multi_language/extend_english.py`, `extend_traditional_chinese.py`
- **Checks**: ruff passes; the cursor, JSON Format, HAR Import, Response Inspector and language-parity tests pass (93); the four labels read as before.
- **Open items**: none.

## U-20260925-127 · 2026-09-25 · The wait cursor shows before the work starts; HAR Import's file read is under it too · #ux #tools

- **What**: from the same review: `busy_cursor()` set the override cursor and went straight into the work. Windows changes the cursor at once, but X11, Wayland and macOS apply a cursor change as their event loop runs, so there it could show only once the work was over (not seen here: this machine and CI run Windows). And HAR Import's Open read the file (`read_text_capped`, up to its size cap, possibly from a slow drive) before `load_text`, outside the cursor.
- **Change**: `busy_cursor()` handles the events waiting once, user input excluded (`QEventLoop.ProcessEventsFlag.ExcludeUserInputEvents`), after setting the cursor and before the block; `HarImportGUI.open_file` reads the file under it.
- **Tests**: `test_busy_cursor.py`: a zero-delay timer set before the block has fired inside it.
- **Files**: `pybreeze/pybreeze_ui/busy_cursor.py`, `pybreeze/pybreeze_ui/tools_gui/har_import_gui.py`, `test/test_utils/test_busy_cursor.py`, `architecture_explore.md` (test count)
- **Checks**: ruff passes; the cursor, HAR Import, Tools-menu, automation menu and JupyterLab lifecycle tests pass (71). `test/test_utils` collects 2729 tests in 152 files.
- **Open items**: none.

## U-20260925-128 · 2026-09-25 · The published 1.0.33 starts from its installed files, with its icon · #verify #packaging

- **What**: every test runs the IDE from the source tree; the release CI made from PR #137 (pybreeze 1.0.33 on PyPI) had not been started from what `pip` installs.
- **Checks**: the 1.0.33 wheel installed with `pip install --no-deps --target` into a folder of its own, its dependencies taken from this repository's `.venv`, started from an empty working folder: `pybreeze` imported from the installed folder, `open_main_window()` built the window with its icon (`pybreeze_icon.ico` read from the installed package) and the title PyBreeze, and the application closed cleanly. A full `pip install pybreeze==1.0.33` into a fresh venv was tried too and stopped: PyPI served PySide6-Addons (168 MB) too slowly to finish in the hour allowed; resolving the dependencies is what CI does from `requirements.txt` on every run.
- **Files**: none (this records a check)
- **Open items**: none.

## U-20260925-129 · 2026-09-25 · CI caches pip's downloads for the unit-test legs · #ci

- **What**: every unit-test leg downloaded its whole dependency set again, PySide6 and its add-ons, JupyterLab and the automation packages among them: "Install dependencies" took 143–192 s of each leg's roughly ten minutes on the release run of PR #137 (run 36130816490).
- **Change**: `actions/setup-python` in both workflows' `unit-tests` job gets `cache: pip`, keyed on `requirements.txt` (and `dev_requirements.txt` in `dev.yml`). Only the downloaded files are cached: pip still resolves every version from PyPI on each run, so the unpinned dependencies are tested at their newest, as before. The publish job is unchanged. `CLAUDE.md` (Branching & CI) and `architecture_explore.md` §18 say so.
- **Files**: `.github/workflows/dev.yml`, `.github/workflows/stable.yml`, `CLAUDE.md`, `architecture_explore.md`
- **Checks**: actionlint 1.7.12 passes both workflows (it passed them before too). The saving shows only once CI runs them; the first run on each Python fills the cache.
- **Open items**: none.

## U-20260925-130 · 2026-09-25 · The JupyterLab tab passes its empty token under jupyter_server 2's names too · #fix #jupyter

- **What**: the tab starts its server with no token and no password through `--ServerApp.token=` and `--ServerApp.password=`, jupyter_server 1.x's names. Started with the tab's flags on jupyter_server 2.17 (this `.venv`), the server logs `ServerApp.token config is deprecated in 2.0. Use IdentityProvider.token.`; a release that stops reading the old name would make a token of its own, and the tab would load a login page.
- **Change**: the server is also given `--IdentityProvider.token=` and `--PasswordIdentityProvider.hashed_password=`. The old two stay, for a jupyter_server 1.x in an older environment. `CLAUDE.md` (Security › JupyterLab) and `architecture_explore.md` name both.
- **Tests**: `test_jupyter_lifecycle.py`: the server's command line carries all four, empty.
- **Files**: `pybreeze/pybreeze_ui/jupyter_lab_gui/jupyter_lab_thread.py`, `test/test_utils/test_jupyter_lifecycle.py`, `CLAUDE.md`, `architecture_explore.md` (JupyterLab tab, test count)
- **Checks**: started for real on jupyter_server 2.17 / JupyterLab 4.5.6 with only the new names, and with all four: `/api/status` answered 200 without a token each time, and the log has no error (with the old names, the one deprecation warning). The JupyterLab lifecycle and readiness tests pass (29). `test/test_utils` collects 2730 tests in 152 files.
- **Open items**: none.

## U-20260925-131 · 2026-09-25 · The SSH terminal's error stream, a failed send, Disconnect and the shell's opening are tested · #test #ssh

- **What**: the SSH and SFTP tests (247) left 10 % of `ssh_command_widget.py` untested, among it paths a user takes: the reader forwarding what the shell writes to its error stream, a command whose send fails, the Disconnect button, and `open_shell_channel` (keepalive, pty size, non-blocking channel).
- **Change**: `TestWhatTheOtherPathsDo` in `test_ssh_terminal_output.py`: error-stream output reaches the terminal like the rest; a send that raises (`Socket is closed`) says so in the terminal and leaves the typed command in the line; Disconnect closes the channel and the client, forgets both, shows the disconnected status and signals the change once; the shell is opened after setting the transport's keepalive, as an `xterm` of the size asked, and made non-blocking.
- **Files**: `test/test_utils/test_ssh_terminal_output.py`, `architecture_explore.md` (test count)
- **Checks**: the four tests pass as the code stands; the file's 54 pass. `test/test_utils` collects 2734 tests in 152 files.
- **Open items**: none.

## U-20260925-132 · 2026-09-25 · The SFTP tree's connect: missing input and a root it cannot list are tested · #test #ssh

- **What**: in the same coverage run, `ssh_file_viewer_widget.py` (the SFTP tree) was at 91 %; untested were two things a user meets on connecting: Connect with no host or no user, and a session that comes up but cannot list `/`.
- **Change**: `TestConnecting` in `test_sftp_tree_actions.py`: a blank host (spaces only) with a user asks for the missing input and starts no connect thread; when listing the root fails after the connect (`Permission denied`), the failure is reported as a failed connect naming the reason, and the state change is signalled once.
- **Files**: `test/test_utils/test_sftp_tree_actions.py`, `architecture_explore.md` (test count)
- **Checks**: both pass as the code stands; the file's 41 pass. `test/test_utils` collects 2736 tests in 152 files.
- **Open items**: none.

## U-20260925-133 · 2026-09-25 · The diagram editor's New asks only about unsaved changes, as Open and Close do · #fix #diagram

- **What**: New asked "Discard current diagram?" whenever anything was on the canvas, a diagram just saved or just opened included, while Open and closing the tab, the dock or the IDE ask only when there are changes that are not saved (`_may_discard_edits`, the undo stack not clean). The two diagram tests on the untested part of `diagram_editor_widget.py` found it.
- **Change**: `_new_diagram` goes through `_may_discard_edits` with its own question ("The diagram has changes that are not saved. Start a new one and lose them?" / 架構圖還有沒存的變更。要新建一個並捨棄它們嗎？), keeping the key `diagram_editor_confirm_new`; `diagram_editor_confirm_title`, used by nothing else, is gone from both dictionaries, which now carry 759 keys each (the three READMEs and `architecture_explore.md` say so). The Tools page, in English and Chinese, says New asks when there are changes not saved.
- **Tests**: `test_unsaved_on_close.py`: after a save, New clears the canvas and the path without a question (it asked once before the change); with an unsaved change it asks, keeps the diagram on No, and on Yes clears it with a clean undo history.
- **Files**: `pybreeze/pybreeze_ui/diagram_editor/diagram_editor_widget.py`, `pybreeze/extend_multi_language/extend_english.py`, `extend_traditional_chinese.py`, `test/test_utils/test_unsaved_on_close.py`, `README.md`, `README/README_zh-TW.md`, `README/README_zh-CN.md`, `docs/source/Eng/menu_tools.rst`, `docs/source/Zh/menu_tools.rst`, `architecture_explore.md` (key count, test count)
- **Checks**: ruff passes; the unsaved-changes, language-parity and diagram editor tests pass (58) and the docs markup test (26). `test/test_utils` collects 2738 tests in 152 files.
- **Open items**: none.

## U-20260925-134 · 2026-09-25 · A test keeps unused keys out of PyBreeze's dictionaries · #test #i18n

- **What**: U-20260925-133 found `diagram_editor_confirm_title` left in both dictionaries after the only code asking for it had gone. A sweep of all 759 keys then: 623 are named in the package outside the language files, and the other 136 all belong to families whose names are built as the IDE runs (`error_text_` + a tag, `run_window_` + a notice, `header_finding_` + a code, `http_status_category_` + a class, the SFTP tree's menu, headers and types, and JEditor's Plugin Browser's `plugin_browser_*`); within each family every suffix is one the code produces.
- **Change**: `test_translation_keys_used.py` (new) fails on a key of `pybreeze_english_word_dict` that the package does not name in quotes and that belongs to none of those families (listed in the test with where each is built). The Traditional Chinese dictionary has the same keys (`test_language_parity.py`).
- **Files**: `test/test_utils/test_translation_keys_used.py` (new), `architecture_explore.md` (test count)
- **Checks**: it passes; with a key nothing asks for added to the English dictionary, it fails naming it (then removed). `test/test_utils` collects 2739 tests in 153 files.
- **Open items**: none.
