# 2026-09 update log, continued a fourth time

Continues [2026-09-d.md](2026-09-d.md), which was nearing the batch size limit. Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260926-72 · 2026-09-26 · Start the 2026-09-e batch · #docs

- **What**: `2026-09-d.md` had reached 740 lines, close to the batch size limit of about 800 (Batch rules, item 2), with this session still recording. Its 91 entries, the batch table's count and the index rows agree.
- **Change**: new entries go into `2026-09-e.md`, listed in the batch table.
- **Files**: `docs/updates/2026-09-e.md` (new), `docs/updates/README.md`
- **Open items**: none.

## U-20260926-73 · 2026-09-26 · A proxy named after its host, and a 300 answer, are tested; the public_http mutation run is read to the end · #test #security #network

- **What**: the mutation run on `utils/network/public_http.py` (47 mutants) finished with 14 alive: 11 change only an annotation; `and` made `or` in `overall_deadline` was killed in U-20260926-70; the deadline timer made non-daemon matters only at interpreter exit. The last one compared a request's host with its URL's host by order (`<`) instead of by difference (`!=`) in `_through_a_proxy`: the proxy tests all used `127.0.0.1`, which sorts before any name, so a proxy named after its target (`zproxy.test` for `rebind.test`) would have been passed by and its target connected to directly. On the `http_client.py` run, `succeeded` counting a 300 as an answer survived: the tests of what is not an answer skipped 300.
- **Change**: `test_public_http.py`: urllib through a proxy named `zproxy.test` (resolving to the local listener) fetches `http://rebind.test/i.png` through it. `test_http_client.py`: 300 joins the statuses that are not an answer.
- **Files**: `test/test_utils/test_public_http.py`, `test/test_utils/test_http_client.py`, `architecture_explore.md` (test count)
- **Checks**: both pass as the code stands; with each mutant applied in a temporary worktree, its test fails. The two files' 70 pass. `test/test_utils` collects 2962 tests in 161 files.
- **Open items**: none.

## U-20260926-74 · 2026-09-26 · A new grid cell size is tested to move the lines while the grid is shown · #test #diagram

- **What**: `DiagramView.grid_size` redraws the grid when it changes while the grid is shown (it drops the cached background and repaints); the tests set the size with the grid off only, so the branch had no coverage.
- **Change**: `test_diagram_view_zoom_grid.py`: with the grid on, a cell size of 30 draws a line at x = 30 and none at x = 20.
- **Files**: `test/test_utils/test_diagram_view_zoom_grid.py`, `architecture_explore.md` (test count)
- **Checks**: it passes as the code stands; the file's 10 pass. `test/test_utils` collects 2963 tests in 161 files.
- **Open items**: none.

## U-20260926-75 · 2026-09-26 · Settings JSON that is not an object, a pixel-sized font, a step answering twice and a second lab load are tested · #test

- **What**: four branches with no test: `load_setting` for prthinker settings holding valid JSON that is not an object (a list), `use_fixed_pitch_font` on a view whose size a style sheet gave in pixels (no point size to carry over), the CoT panel receiving a second answer for a step it already lists, and the JupyterLab tab told the lab is ready a second time, with its status line already gone.
- **Change**: `test_prthinker_setting.py`: a JSON list reads as the defaults. `test_terminal_font.py`: a 17-pixel font becomes the fixed-pitch font at a real point size. `test_cot_session_reuse.py`: a second answer replaces the first under one selector entry and is what is shown. `test_jupyter_ready.py`: a second `load_lab` loads the URL again and raises nothing.
- **Files**: `test/test_utils/test_prthinker_setting.py`, `test/test_utils/test_terminal_font.py`, `test/test_utils/test_cot_session_reuse.py`, `test/test_utils/test_jupyter_ready.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; the four files' 109 pass. `test/test_utils` collects 2966 tests in 161 files.
- **Open items**: none.

## U-20260926-76 · 2026-09-26 · The read-deadline test's clock starts at 1000, so a deadline computed from the wrong start is caught · #test #network

- **What**: in the mutation run on `http_client.py`, computing the read deadline as `monotonic() * max_seconds` instead of `+` survived: the tests' stand-in clocks started at 0, where the product and the sum both give a deadline of 0 or 300 that the test then passed. With a real clock the product is far off, and only the watchdog would stop a slow answer. The run's other late survivors are equivalent or already covered: a `/` in the signature, the watchdog thread made non-daemon (it is cancelled at the end), and `< 301` (the 300 case of U-20260926-73).
- **Change**: `test_reading_stops_at_the_first_chunk_past_the_deadline` in `test_http_client.py` starts its clock at 1000, with a note why.
- **Files**: `test/test_utils/test_http_client.py`
- **Checks**: the file's 41 pass; with the mutant applied in a temporary worktree the test fails. The test count is unchanged (2966).
- **Open items**: none.

## U-20260926-77 · 2026-09-26 · The host-key question box's answer, and a No forgotten after ten seconds, are tested · #test #security #ssh

- **What**: early survivors of the mutation run on `ssh_host_key_policy.py`. Every test stood in for `HostKeyAsker`, so its message box never ran: an answer read the wrong way round (trusting a key on No) went unseen. And a No is remembered for ten seconds so that the other half of one Connect is refused without asking twice; with the elapsed time computed as a quotient instead of a difference, a No would have been remembered for as long as the IDE ran, and every later Connect to that host refused unasked.
- **Change**: `test_ssh_host_key_policy.py`: with only `QMessageBox.exec` stood in for, Yes trusts the key and No does not; the box starts on No and shows the message as text (`as_text`), not markup. With the clock stood in for, a No given at 1000 s is asked again at 1011 s.
- **Files**: `test/test_utils/test_ssh_host_key_policy.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each mutant applied in a temporary worktree, one or two fail. The file's 18 pass. `test/test_utils` collects 2969 tests in 161 files.
- **Open items**: none; the rest of the run is being read.

## U-20260926-78 · 2026-09-26 · A refused host key is asked about again ten seconds after the user's No, however often Connect was clicked · #fix #ssh #security

- **What**: one Connect starts two SSH connects (the shell and the file tree), so a No given to an unknown host key is remembered for ten seconds and the other half is refused without a second question (`ssh_host_key_policy.py`). Every refusal made from that memory also renewed its time, so the ten seconds slid on: a user who said No by mistake and clicked Connect again within ten seconds, and again, was refused each time without a word and never asked again for as long as they kept trying. Found while tightening the test of U-20260926-77, after a mutation survivor lengthening the window to 11 seconds showed it was not pinned.
- **Change**: only a No the user gives starts the ten seconds; a refusal made from the memory keeps the original time. `architecture_explore.md` says so.
- **Files**: `pybreeze/pybreeze_ui/connect_gui/ssh/ssh_host_key_policy.py`, `test/test_utils/test_ssh_host_key_policy.py`, `architecture_explore.md`
- **Checks**: `test_a_no_is_remembered_for_ten_seconds_and_then_forgotten` (with the clock stood in for): a No at 1000 s, a refusal without asking at 1009.5 s, and the question again at 1010.5 s. It failed before the change (still refused at 1010.5 s, the window having restarted at 1009.5) and passes after; the window lengthened to 11 s now fails it. The SSH policy, loopback, SFTP tree and security tests pass (71).
- **Open items**: none.

## U-20260926-79 · 2026-09-26 · What a terminal read holds back, and the host-key question's buttons, file and log line, are tested · #test #ssh #security

- **What**: survivors of the mutation runs on `terminal_text.py` and `ssh_host_key_policy.py`, the latter's read against the code as it now stands. `test_ansi_escape.py` never called `split_unfinished_end`, and nothing pinned the 256-character limit on an unfinished escape held for the next read: a limit one short (`<` for `<=`) went unseen, and a `\r` held with a cut escape was only covered by the slower SSH and run-window tests. For the host key, the box's buttons were never looked at (`Yes - No` in place of `Yes | No` left a box with neither), the first host stored into a missing file could have started it with a blank line (`or` for `and`), a comment line could have been handed to paramiko (`and` for `or`), and the log named the wrong line for a bad one (numbering from 0). The rest of that run's survivors are type annotations (the module has `from __future__ import annotations`), `==` for `is` between two QThreads, and those U-20260926-77 and -78 already kill.
- **Change**: `test_ansi_escape.py`: a `\r` at the end waits, a `\r` before a cut escape waits with it, an unfinished escape of exactly 256 characters is held and one of 257 is shown as it is. `test_ssh_host_key_policy.py`: the box offers Yes and No only; a known hosts file opening with a comment and a bad line logs `Skipping line 2` and nothing about the comment; the first host trusted is the file's first line.
- **Files**: `test/test_utils/test_ansi_escape.py`, `test/test_utils/test_ssh_host_key_policy.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each mutant applied in a temporary worktree at HEAD, one or two of them fail (the `<` limit, `>>` for `+`, `Yes - No`, `Yes << No`, `Yes / No`, `or`, `and`, numbering from 0, a 9 s window, `<` on the answer). The two files' 46 pass; `test/test_utils` collects 2973 tests in 161 files.
- **Open items**: none; the rest of the terminal run is being read.

## U-20260926-80 · 2026-09-26 · A trusted host that comes back with another key is refused without a question, tested against a real server · #test #ssh #security

- **What**: trust on first use is only worth something if the second use checks: a host trusted once that later shows another key (someone in between, or a rebuilt server) must be refused, not asked about as a new host, and its trusted key must stay. No test covered that; the policy tests call `missing_host_key` directly, which paramiko never reaches for a known host. The loopback server helper also let a client that hangs up in the handshake (as one refusing the key does) end its accept thread with `ConnectionResetError`, which pytest reported as an unhandled thread exception in about one run in three.
- **Change**: `test_ssh_loopback.py`: connect once (the key is trusted), give the server a new key, connect again: `BadHostKeyException`, no session, no second question, and the known hosts file byte for byte as it was. `ssh_loopback_server.py` goes on accepting after an `OSError` or `EOFError` in the handshake. `architecture_explore.md` names the case.
- **Files**: `test/test_utils/test_ssh_loopback.py`, `test/test_utils/ssh_loopback_server.py`, `architecture_explore.md`
- **Checks**: the test passes as the code stands, six runs in a row with `PytestUnhandledThreadExceptionWarning` made an error; the two loopback files' 25 pass. `test/test_utils` collects 2974 tests. The terminal_text mutation run finished: 89 mutants, 2 survivors, both killed by U-20260926-79.
- **Open items**: none.

## U-20260926-81 · 2026-09-26 · A host key other than the trusted one is said so, with both fingerprints and the file to edit; a key login no longer calls every failure a key failure · #fix #ssh #security #i18n #docs

- **What**: a host trusted before that shows another key is refused by paramiko (`BadHostKeyException`) before the host-key policy is reached. The user was shown paramiko's text: both keys whole in base64, in English, and nothing on what it may mean or what to do. And in the shell tab a login with a key file wrapped everything its connect raised as "Key auth failed": a changed host key, a host key the user had just declined (whose message then went untranslated, `error_text` finding no constant at its start), or a host that could not be reached all read as a key the server refused.
- **Change**: `exception_tags.host_key_changed_error` (translated in `extend_traditional_chinese.py`) says the key has changed, gives the SHA256 fingerprint now shown and the one trusted before, that someone may be intercepting the connection, and, if the key was changed on purpose, which file to remove the host's line from: PyBreeze's `ssh_known_hosts` when its line for this host (on any port, `[host]:port`) holds the trusted key, `~/.ssh/known_hosts` otherwise; the same key trusted under another name (the server's address, say) is not this host's line (`ssh_host_key_policy.changed_host_key_message`). `SshConnectThread` reports a `BadHostKeyException` with it, and logs a warning. `SSHCommandWidget._connect_with_key` says "Key auth failed" only for a key file that cannot be read or a key the server refuses (`AuthenticationException`); anything else passes as it is. README (three languages) and the SSH guide (English and Chinese) say what happens to a changed key, and that hosts in `~/.ssh/known_hosts` are trusted too; the READMEs and `architecture_explore.md` count 760 language keys.
- **Files**: `pybreeze/utils/exception/exception_tags.py`, `pybreeze/extend_multi_language/extend_traditional_chinese.py`, `pybreeze/pybreeze_ui/connect_gui/ssh/ssh_host_key_policy.py`, `ssh_connect_thread.py`, `ssh_command_widget.py`, `test/test_utils/test_ssh_host_key_policy.py`, `test/test_utils/test_ssh_loopback.py`, `README.md`, `README/README_zh-TW.md`, `README/README_zh-CN.md`, `docs/source/Eng/ssh_client.rst`, `docs/source/Zh/ssh_client.rst`, `architecture_explore.md`
- **Checks**: against the loopback SSH server, with a key file: a key the server does not take still reads "Key auth failed"; a trusted host given a new key shows the changed-key message with both fingerprints and PyBreeze's file, and no "Key auth failed". That test failed on the code before the change ("Key auth failed Host key for server '127.0.0.1' does not match: got 'AAAAB3…'"). The message names PyBreeze's file for a host trusted there on another port, and the system file when PyBreeze's holds nothing for the host, the same key under another name, or another key for it. `test_error_text.py` finds the new constant again from a message, in Chinese. The SSH, SFTP, error text, language, import and docs tests pass (797, and 41 in the two host-key files after the per-host check); `test/test_utils` collects 2982 tests.
- **Open items**: none.

## U-20260926-82 · 2026-09-26 · The host-key question is tested with an open panel, and the ten-second No from a clock starting at 5 · #test #ssh #security

- **What**: two more survivors of the mutation run on `ssh_host_key_policy.py`, checked against the code as it now stands. `HostKeyAsker._show` drops the question of a panel that has been closed; with `and` turned to `or` it dropped every panel's question and answered No, and nothing noticed, the only test of the box asking with no panel at all. And the ten-second memory of a No computed as the remainder `monotonic() % declined_at` instead of the difference passed U-20260926-78's test, whose clock started at 1000: 1009.5 % 1000 and 1010.5 % 1000 are the differences themselves. The run's other survivors are annotations, `==` for `is` between QThreads, `<=` for `==` on the answer (a box with Yes and No gives only those two), `<=` at exactly ten seconds, a `[host]` prefix without its colon, and those already killed by U-20260926-77, -78 and -79.
- **Change**: `test_ssh_host_key_policy.py`: an open panel's question is shown, the box has the panel as its parent, and Yes is Yes; the ten-second test runs from a clock at 5 as well as at 1000.
- **Files**: `test/test_utils/test_ssh_host_key_policy.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each mutant applied in a temporary worktree, one fails (`%`, and `or` in `_show`). The file's 26 pass; `test/test_utils` collects 2984 tests.
- **Open items**: none.

## U-20260926-83 · 2026-09-26 · The one host-key asker, made on first use on the UI thread, is tested; the host-key mutation run is read to its end · #test #ssh

- **What**: the last survivors of the mutation run on `ssh_host_key_policy.py` (198 mutants, 82 surviving in the worktree the run started from). Every test stands in for `host_key_asker()`, so with its `is None` turned to `is not None` it returned None and every real question would have failed, unseen. Read against the code as it now stands, the other survivors are killed by U-20260926-77, -78, -79 and -82 or are equivalent: type annotations; `is` and `==` between QThreads or between the answer and Yes; `+` and `^` joining Yes and No (two distinct bits); `<=` and `>=` against Yes where the box gives only Yes or No; the constructor's first answer, which `ask` resets; exactly ten seconds; and a `[host]` prefix without its colon.
- **Change**: `test_there_is_one_asker_made_on_first_use` in `test_ssh_host_key_policy.py`: made on the first call, the same one on the next, living on the UI thread.
- **Files**: `test/test_utils/test_ssh_host_key_policy.py`, `architecture_explore.md` (test count)
- **Checks**: passes as the code stands; fails with either mutant (`is not None`, `not ... is None`) applied in a temporary worktree. The file's 27 pass; `test/test_utils` collects 2985 tests.
- **Open items**: none.

## U-20260926-84 · 2026-09-26 · The automation menus' Run entries come from one function; the six copied executor modules are gone · #refactor #menu #docs

- **What** (refactor, no change in behaviour): `extend/process_executor/` held a sub-package per automation package (`api_testka`, `auto_control`, `file_automation`, `load_density`, `web_runner`), each a 42-line copy of the others differing only in its `_PACKAGE` constant, with four functions that passed their arguments on to `build_process()` or `run_dir_files_with_package()`; `mail_thunder` held one. Each menu then wrote out the same four `RunAction`s over them. Nothing outside the menus imported them (checked across the workspace); `architecture.md` §6 lists none of them.
- **Change**: `automation_menu_factory.package_run_actions(ui, label_prefix, package)` makes the four Run entries (the script, with the report mailed, a folder, a folder with the report mailed), labelled with the prefix and `RUN_ENTRY_LABEL_SUFFIXES`; the five menus pass it their prefix and package, and MailThunder's single entry calls `build_process` itself. The six sub-packages are deleted. `test_automation_run_entries.py` records the runs where the entries now look them up, with stand-ins that take what the real functions take, and checks each entry has a label of its own; `test_language_keys_used.py` counts a key built by `package_run_actions` from a prefix a menu passes it as used. `CLAUDE.md` (tree and patterns), `architecture.md` (layers, the run flow, the new-package extension point), `architecture_explore.md` (§4.3, the layer diagram, §5.1, the pattern table) and the three READMEs' trees follow.
- **Files**: `pybreeze/pybreeze_ui/menu/automation_menu/automation_menu_factory.py`, the six `build_*_menu.py` under `automation_menu/`, `pybreeze/extend/process_executor/{api_testka,auto_control,file_automation,load_density,web_runner,mail_thunder}/` (deleted), `test/test_utils/test_automation_run_entries.py`, `test/test_utils/test_language_keys_used.py`, `CLAUDE.md`, `architecture.md`, `architecture_explore.md`, `README.md`, `README/README_zh-TW.md`, `README/README_zh-CN.md`
- **Checks**: the run entries test asserts the same runs as before for every package (script and folder, with and without the report) and MailThunder's one; the language, packaging, startup-import and menu tests pass (47), and so does the whole of `test/test_utils`; the IDE starts and the extend tab loads (`start_automation_test.py`, `extend_automation_test.py`, both exit 0). The test count is unchanged (2985).
- **Open items**: none.

## U-20260926-85 · 2026-09-26 · A JWT without its signature part, stray characters that leave the padding alone, the first of two tokens and the fallback layout are tested · #test #tools

- **What**: survivors of the mutation run on `jwt_decoder.py` (117 mutants, 19 surviving). With the segment count checked as `> 3` instead of `!= 3`, a token without its signature part (two parts that both decode) read past the end of the list and raised `IndexError` out of the JWT tab's Decode; the one test of a wrong count used two parts that do not decode, which fail anyway. With `validate=False` a stray character was still refused, but only because one of them changes the padding: four of them decoded. The layout of a segment shown from its decoded value (a repeated claim) was checked only as JSON, so any indent passed; and with two tokens in the pasted text, the last could have been decoded instead of the first. The other survivors are equivalent: annotations, a `/` in `shown_json`'s signature, `is not 3` (a small int), and `<= 1` for a stripped, non-empty line's word count.
- **Change**: `test_jwt_decoder.py`: a wrong count is refused with the malformed-token message, and so is a token whose two parts decode but whose signature part is missing; four stray characters are refused as one is; of two tokens the first is decoded; the fallback layout is compared in full (four spaces a level).
- **Files**: `test/test_utils/test_jwt_decoder.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each mutant applied in a temporary worktree one or two fail (`> 3`, `validate=False`, `indent=5`, `found[-1]`). The file's 47 pass; `test/test_utils` collects 2988 tests.
- **Open items**: none.

## U-20260926-86 · 2026-09-26 · The run window's read size is one named constant, not nine copies of 1024000 · #refactor #process

- **What** (refactor, no change in behaviour): the most bytes a run window's reader takes from the child's pipe at a time was written as the literal `1024000` in seven signatures of `process_executor_utils.py` and two of `prthinker_process.py`, a number repeated with nothing saying what it is (`CLAUDE.md`: a repeated magic number becomes a named constant).
- **Change**: `process_executor_utils.DEFAULT_PROGRAM_BUFFER`, with a comment, is the default of every `program_buffer` there and in `prthinker_process.py`. TestPioneer's run (`1024`) and the `TaskProcessManager` and `FileRunnerProcess` defaults are left as they are.
- **Files**: `pybreeze/extend/process_executor/process_executor_utils.py`, `pybreeze/extend/process_executor/prthinker/prthinker_process.py`
- **Checks**: ruff passes; the executor, run window, prthinker, automation and report tests pass (460, 14 skipped without prthinker).
- **Open items**: none.

## U-20260926-87 · 2026-09-26 · Header merging has tests of its own: another name is kept apart, and only Cookie is joined with a semicolon · #test #tools

- **What**: `header_merge.py` combines the headers the cURL and HAR importers collect, and was tested only through them. In the mutation run on it (36 mutants, 13 surviving, 11 of them annotations) comparing names with `>=` instead of `==` survived, which would have merged `Accept` into a `User-Agent` collected before it, and so did choosing the joiner with `>=`, which would have joined any header after "cookie" in the alphabet (`X-Tag`) with "; ".
- **Change**: `test_header_merge.py` (new): a stored name is found in any casing and another name is not, whether it sorts before or after; headers of other names stay apart and a repeated one is combined under its first spelling; Cookie is joined with "; " and headers before and after it in the alphabet with ", "; an empty side is dropped; a default is set only when the header is absent.
- **Files**: `test/test_utils/test_header_merge.py`, `architecture_explore.md` (test count)
- **Checks**: the 8 pass as the code stands; with each mutant applied in a temporary worktree, 3 and 1 fail. `test/test_utils` collects 2996 tests in 162 files.
- **Open items**: none.

## U-20260926-88 · 2026-09-26 · Each character a text view does not keep is tested as escaped, its neighbours as kept, and the rest as written · #test #tools

- **What**: the mutation run on `view_safe.py` (70 mutants, 9 surviving). The tests sent the whole set of characters a Qt view does not give back through a view together and checked that the JSON meant the same afterwards; the view keeps U+0085 and U+2028 through `exact_text`, and a lone surrogate from the middle of the range stood for all of them, so the code points could move by one (U+0084, U+2027 or U+2029 in place of U+2028, U+D7FF or U+D801 as the range's start, U+DFFE or U+E000 as its end) and nothing failed. Nor was `ensure_ascii=False` held: with `True` every non-ASCII character, Chinese included, would have been shown as a `\uXXXX` escape.
- **Change**: `test_view_safe.py`: each of U+0085, U+2028, U+2029, U+FDD0, U+FDD1 and U+D800, U+DBFF, U+DC00, U+DFFF is written as its escape; U+0084, U+0086, U+2027, U+202A, U+D7FF, U+E000, U+FDCF and U+FDD2 are left as they are; `dumps_for_view` writes Chinese and accented text as it is.
- **Files**: `test/test_utils/test_view_safe.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each of the nine mutants applied in a temporary worktree one or two fail. The file's 26 pass; `test/test_utils` collects 3014 tests.
- **Open items**: none.

## U-20260926-89 · 2026-09-26 · An extended colour after other SGR parameters, a parameter too long to read and one of five digits are tested · #test #ssh

- **What**: early survivors of the mutation run on `terminal_style.py` (749 mutants, the palette tables' 304 number replacements filtered out). Every extended colour in the tests came first in its sequence (`38;5;196`), where the index read after the form is the same whether it is `start + 1` or `start << 1`; after a bold (`1;38;5;196`) the second reads the wrong parameter. A parameter longer than five digits is read as `_UNKNOWN` (-1) and ignored; with the reset tested as `<= 0` instead of `== 0` it would have wiped the style. And a parameter of exactly five digits was never sent, so reading only four passed. `number % first` for `number - first` in the basic colour runs is equivalent (each run lies below twice its start), and the rest are annotations.
- **Change**: `test_terminal_style.py`: `1;38;5;196` and `1;4;48;2;10;20;30` set the colour with the emphasis; `123456` leaves a bold style bold; `00031` is red.
- **Files**: `test/test_utils/test_terminal_style.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each of the three mutants applied in a temporary worktree one fails. The file's 50 pass; `test/test_utils` collects 3018 tests.
- **Open items**: none; the rest of the run is being read.

## U-20260926-90 · 2026-09-26 · A query with an empty value round-trips, and the Query to JSON layout is compared in full · #test #tools

- **What**: the mutation run on `query_convert.py` (53 mutants, 27 surviving, 22 of them annotations). `query_round_trips` decides whether the cURL and HAR import split a query into `params`; with `keep_blank_values=False` an empty value (`a=&b=1`) would have been dropped in the check and such a query kept as text, which no test had. The Query to JSON output was checked only for a newline, so unsorted keys or another indent passed. `error.args[-1]` for `args[0]` is equivalent (a `DuplicateKeyError` carries only the key).
- **Change**: `test_query_convert.py`: `a=&b=1` round-trips; `query_to_json("b=2&a=1")` is compared with its whole text (keys sorted, four spaces a level).
- **Files**: `test/test_utils/test_query_convert.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each of the three mutants applied in a temporary worktree one fails. The file's 40 pass; `test/test_utils` collects 3019 tests.
- **Open items**: none.

## U-20260926-91 · 2026-09-26 · pretty_json_or_none has tests of its own: its layout, characters and numbers as written, and what it turns away · #test #tools

- **What**: `pretty_json_or_none` lays out a response body or a JWT segment, and was tested only through the tools that call it. In the mutation run on `json_process.py` it survived written with `ensure_ascii=True` (Chinese shown as `\uXXXX` escapes), with an indent of three, and with its `except` no longer taking `RecursionError`, which would have let text nested past the recursion limit escape from the Response Inspector and the JWT tab. The run's other survivors so far are annotations, the length of the random number marker (`token_hex(8)`, any length works) and `args[-1]` for a `DuplicateKeyError`'s only argument.
- **Change**: `TestPrettyJsonOrNone` in `test_json_process.py`: a Chinese key and `1e400` come out as written, four spaces a level; keys are sorted only when asked; text that is not JSON, a repeated key, `NaN` and brackets nested a hundred thousand deep give `None`.
- **Files**: `test/test_utils/test_json_process.py`, `architecture_explore.md` (test count)
- **Checks**: they pass as the code stands; with each of the three mutants applied in a temporary worktree one or two fail. The file's 39 pass; `test/test_utils` collects 3025 tests.
- **Open items**: none; the rest of the run is being read.
